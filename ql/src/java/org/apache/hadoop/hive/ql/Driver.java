begin_unit|revision:0.9.5;language:Java;cregit-version:0.0.1
begin_comment
comment|/*  * Licensed to the Apache Software Foundation (ASF) under one  * or more contributor license agreements.  See the NOTICE file  * distributed with this work for additional information  * regarding copyright ownership.  The ASF licenses this file  * to you under the Apache License, Version 2.0 (the  * "License"); you may not use this file except in compliance  * with the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_package
package|package
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
package|;
end_package

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|ByteArrayOutputStream
import|;
end_import

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|DataInput
import|;
end_import

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|IOException
import|;
end_import

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|PrintStream
import|;
end_import

begin_import
import|import
name|java
operator|.
name|io
operator|.
name|Serializable
import|;
end_import

begin_import
import|import
name|java
operator|.
name|net
operator|.
name|InetAddress
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|ArrayList
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|Collection
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|HashMap
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|HashSet
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|LinkedHashMap
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|LinkedHashSet
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|LinkedList
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|List
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|Map
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|Queue
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|Set
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|concurrent
operator|.
name|atomic
operator|.
name|AtomicBoolean
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|concurrent
operator|.
name|locks
operator|.
name|ReentrantLock
import|;
end_import

begin_import
import|import
name|java
operator|.
name|util
operator|.
name|stream
operator|.
name|Collectors
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|commons
operator|.
name|lang
operator|.
name|StringUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|commons
operator|.
name|lang3
operator|.
name|tuple
operator|.
name|ImmutablePair
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|commons
operator|.
name|lang3
operator|.
name|tuple
operator|.
name|Pair
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|conf
operator|.
name|Configurable
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|fs
operator|.
name|FSDataInputStream
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|common
operator|.
name|JavaUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|common
operator|.
name|ValidTxnList
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|common
operator|.
name|ValidTxnWriteIdList
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|common
operator|.
name|ValidWriteIdList
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|common
operator|.
name|metrics
operator|.
name|common
operator|.
name|Metrics
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|common
operator|.
name|metrics
operator|.
name|common
operator|.
name|MetricsConstant
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|common
operator|.
name|metrics
operator|.
name|common
operator|.
name|MetricsFactory
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|conf
operator|.
name|HiveConf
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|conf
operator|.
name|HiveConf
operator|.
name|ConfVars
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|conf
operator|.
name|HiveVariableSource
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|conf
operator|.
name|VariableSubstitution
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|metastore
operator|.
name|HiveMetaStoreUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|metastore
operator|.
name|Warehouse
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|metastore
operator|.
name|api
operator|.
name|FieldSchema
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|metastore
operator|.
name|api
operator|.
name|LockComponent
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|metastore
operator|.
name|api
operator|.
name|LockType
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|metastore
operator|.
name|api
operator|.
name|Schema
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|metastore
operator|.
name|utils
operator|.
name|MetaStoreUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|cache
operator|.
name|results
operator|.
name|CacheUsage
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|cache
operator|.
name|results
operator|.
name|QueryResultsCache
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|cache
operator|.
name|results
operator|.
name|QueryResultsCache
operator|.
name|CacheEntry
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|ddl
operator|.
name|DDLDesc
operator|.
name|DDLDescWithWriteId
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|AbstractFileMergeOperator
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|ConditionalTask
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|DagUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|ExplainTask
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|FetchTask
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|Operator
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|TableScanOperator
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|Task
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|TaskFactory
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|TaskResult
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|TaskRunner
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|Utilities
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|repl
operator|.
name|util
operator|.
name|ReplUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|exec
operator|.
name|spark
operator|.
name|session
operator|.
name|SparkSession
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|history
operator|.
name|HiveHistory
operator|.
name|Keys
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|hooks
operator|.
name|Entity
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|hooks
operator|.
name|HookContext
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|hooks
operator|.
name|HookUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|hooks
operator|.
name|PrivateHookContext
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|hooks
operator|.
name|ReadEntity
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|hooks
operator|.
name|WriteEntity
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|io
operator|.
name|AcidUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|lock
operator|.
name|CompileLock
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|lock
operator|.
name|CompileLockFactory
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|lockmgr
operator|.
name|HiveLock
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|lockmgr
operator|.
name|HiveLockMode
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|lockmgr
operator|.
name|HiveTxnManager
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|lockmgr
operator|.
name|LockException
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|log
operator|.
name|PerfLogger
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|metadata
operator|.
name|AuthorizationException
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|metadata
operator|.
name|Hive
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|metadata
operator|.
name|HiveException
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|metadata
operator|.
name|Table
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|metadata
operator|.
name|formatting
operator|.
name|JsonMetaDataFormatter
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|metadata
operator|.
name|formatting
operator|.
name|MetaDataFormatUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|metadata
operator|.
name|formatting
operator|.
name|MetaDataFormatter
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|ASTNode
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|BaseSemanticAnalyzer
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|ExplainConfiguration
operator|.
name|AnalyzeState
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|HiveSemanticAnalyzerHookContext
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|HiveSemanticAnalyzerHookContextImpl
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|ParseException
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|ParseUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|parse
operator|.
name|SemanticAnalyzerFactory
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|plan
operator|.
name|FileSinkDesc
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|plan
operator|.
name|HiveOperation
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|plan
operator|.
name|TableDesc
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|plan
operator|.
name|mapper
operator|.
name|PlanMapper
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|plan
operator|.
name|mapper
operator|.
name|StatsSource
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|processors
operator|.
name|CommandProcessorResponse
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|security
operator|.
name|authorization
operator|.
name|command
operator|.
name|CommandAuthorizer
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|session
operator|.
name|LineageState
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|session
operator|.
name|SessionState
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|session
operator|.
name|SessionState
operator|.
name|LogHelper
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|ql
operator|.
name|wm
operator|.
name|WmContext
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|hive
operator|.
name|serde2
operator|.
name|ByteStream
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|mapreduce
operator|.
name|MRJobConfig
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hive
operator|.
name|common
operator|.
name|util
operator|.
name|ShutdownHookManager
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|hive
operator|.
name|common
operator|.
name|util
operator|.
name|TxnIdUtils
import|;
end_import

begin_import
import|import
name|org
operator|.
name|apache
operator|.
name|thrift
operator|.
name|TException
import|;
end_import

begin_import
import|import
name|org
operator|.
name|json
operator|.
name|JSONObject
import|;
end_import

begin_import
import|import
name|org
operator|.
name|slf4j
operator|.
name|Logger
import|;
end_import

begin_import
import|import
name|org
operator|.
name|slf4j
operator|.
name|LoggerFactory
import|;
end_import

begin_import
import|import
name|com
operator|.
name|google
operator|.
name|common
operator|.
name|annotations
operator|.
name|VisibleForTesting
import|;
end_import

begin_import
import|import
name|com
operator|.
name|google
operator|.
name|common
operator|.
name|base
operator|.
name|Strings
import|;
end_import

begin_import
import|import
name|com
operator|.
name|google
operator|.
name|common
operator|.
name|collect
operator|.
name|ImmutableMap
import|;
end_import

begin_class
specifier|public
class|class
name|Driver
implements|implements
name|IDriver
block|{
specifier|static
specifier|final
specifier|private
name|String
name|CLASS_NAME
init|=
name|Driver
operator|.
name|class
operator|.
name|getName
argument_list|()
decl_stmt|;
specifier|private
specifier|static
specifier|final
name|Logger
name|LOG
init|=
name|LoggerFactory
operator|.
name|getLogger
argument_list|(
name|CLASS_NAME
argument_list|)
decl_stmt|;
specifier|static
specifier|final
specifier|private
name|LogHelper
name|console
init|=
operator|new
name|LogHelper
argument_list|(
name|LOG
argument_list|)
decl_stmt|;
specifier|private
specifier|static
specifier|final
name|int
name|SHUTDOWN_HOOK_PRIORITY
init|=
literal|0
decl_stmt|;
specifier|private
specifier|final
name|QueryInfo
name|queryInfo
decl_stmt|;
specifier|private
name|Runnable
name|shutdownRunner
init|=
literal|null
decl_stmt|;
specifier|private
name|int
name|maxRows
init|=
literal|100
decl_stmt|;
specifier|private
name|ByteStream
operator|.
name|Output
name|bos
init|=
operator|new
name|ByteStream
operator|.
name|Output
argument_list|()
decl_stmt|;
specifier|private
specifier|final
name|HiveConf
name|conf
decl_stmt|;
specifier|private
name|DataInput
name|resStream
decl_stmt|;
specifier|private
name|Context
name|ctx
decl_stmt|;
specifier|private
name|DriverContext
name|driverCxt
decl_stmt|;
specifier|private
name|QueryPlan
name|plan
decl_stmt|;
specifier|private
name|Schema
name|schema
decl_stmt|;
specifier|private
name|String
name|errorMessage
decl_stmt|;
specifier|private
name|String
name|SQLState
decl_stmt|;
specifier|private
name|Throwable
name|downstreamError
decl_stmt|;
specifier|private
name|FetchTask
name|fetchTask
decl_stmt|;
specifier|private
name|List
argument_list|<
name|HiveLock
argument_list|>
name|hiveLocks
init|=
operator|new
name|ArrayList
argument_list|<
name|HiveLock
argument_list|>
argument_list|()
decl_stmt|;
comment|// A limit on the number of threads that can be launched
specifier|private
name|int
name|maxthreads
decl_stmt|;
specifier|private
name|String
name|userName
decl_stmt|;
comment|// HS2 operation handle guid string
specifier|private
name|String
name|operationId
decl_stmt|;
comment|// For WebUI.  Kept alive after queryPlan is freed.
specifier|private
specifier|final
name|QueryDisplay
name|queryDisplay
init|=
operator|new
name|QueryDisplay
argument_list|()
decl_stmt|;
specifier|private
name|LockedDriverState
name|lDrvState
init|=
operator|new
name|LockedDriverState
argument_list|()
decl_stmt|;
comment|// Query specific info
specifier|private
specifier|final
name|QueryState
name|queryState
decl_stmt|;
comment|// Query hooks that execute before compilation and after execution
specifier|private
name|HookRunner
name|hookRunner
decl_stmt|;
comment|// Transaction manager the Driver has been initialized with (can be null).
comment|// If this is set then this Transaction manager will be used during query
comment|// compilation/execution rather than using the current session's transaction manager.
comment|// This might be needed in a situation where a Driver is nested within an already
comment|// running Driver/query - the nested Driver requires a separate transaction manager
comment|// so as not to conflict with the outer Driver/query which is using the session
comment|// transaction manager.
specifier|private
specifier|final
name|HiveTxnManager
name|initTxnMgr
decl_stmt|;
comment|// Transaction manager used for the query. This will be set at compile time based on
comment|// either initTxnMgr or from the SessionState, in that order.
specifier|private
name|HiveTxnManager
name|queryTxnMgr
decl_stmt|;
specifier|private
name|StatsSource
name|statsSource
decl_stmt|;
comment|// Boolean to store information about whether valid txn list was generated
comment|// for current query.
specifier|private
name|boolean
name|validTxnListsGenerated
decl_stmt|;
specifier|private
name|CacheUsage
name|cacheUsage
decl_stmt|;
specifier|private
name|CacheEntry
name|usedCacheEntry
decl_stmt|;
specifier|private
name|ValidWriteIdList
name|compactionWriteIds
init|=
literal|null
decl_stmt|;
specifier|private
name|long
name|compactorTxnId
init|=
literal|0
decl_stmt|;
specifier|private
name|Context
name|backupContext
init|=
literal|null
decl_stmt|;
specifier|private
name|boolean
name|retrial
init|=
literal|false
decl_stmt|;
specifier|private
enum|enum
name|DriverState
block|{
name|INITIALIZED
block|,
name|COMPILING
block|,
name|COMPILED
block|,
name|EXECUTING
block|,
name|EXECUTED
block|,
comment|// a state that the driver enters after close() has been called to clean the query results
comment|// and release the resources after the query has been executed
name|CLOSED
block|,
comment|// a state that the driver enters after destroy() is called and it is the end of driver life cycle
name|DESTROYED
block|,
name|ERROR
block|}
specifier|public
specifier|static
class|class
name|LockedDriverState
block|{
comment|// a lock is used for synchronizing the state transition and its associated
comment|// resource releases
specifier|public
specifier|final
name|ReentrantLock
name|stateLock
init|=
operator|new
name|ReentrantLock
argument_list|()
decl_stmt|;
specifier|public
name|DriverState
name|driverState
init|=
name|DriverState
operator|.
name|INITIALIZED
decl_stmt|;
specifier|public
name|AtomicBoolean
name|aborted
init|=
operator|new
name|AtomicBoolean
argument_list|()
decl_stmt|;
specifier|private
specifier|static
name|ThreadLocal
argument_list|<
name|LockedDriverState
argument_list|>
name|lds
init|=
operator|new
name|ThreadLocal
argument_list|<
name|LockedDriverState
argument_list|>
argument_list|()
block|{
annotation|@
name|Override
specifier|protected
name|LockedDriverState
name|initialValue
parameter_list|()
block|{
return|return
operator|new
name|LockedDriverState
argument_list|()
return|;
block|}
block|}
decl_stmt|;
specifier|public
specifier|static
name|void
name|setLockedDriverState
parameter_list|(
name|LockedDriverState
name|lDrv
parameter_list|)
block|{
name|lds
operator|.
name|set
argument_list|(
name|lDrv
argument_list|)
expr_stmt|;
block|}
specifier|public
specifier|static
name|LockedDriverState
name|getLockedDriverState
parameter_list|()
block|{
return|return
name|lds
operator|.
name|get
argument_list|()
return|;
block|}
specifier|public
specifier|static
name|void
name|removeLockedDriverState
parameter_list|()
block|{
if|if
condition|(
name|lds
operator|!=
literal|null
condition|)
block|{
name|lds
operator|.
name|remove
argument_list|()
expr_stmt|;
block|}
block|}
specifier|public
name|boolean
name|isAborted
parameter_list|()
block|{
return|return
name|aborted
operator|.
name|get
argument_list|()
return|;
block|}
specifier|public
name|void
name|abort
parameter_list|()
block|{
name|aborted
operator|.
name|set
argument_list|(
literal|true
argument_list|)
expr_stmt|;
block|}
annotation|@
name|Override
specifier|public
name|String
name|toString
parameter_list|()
block|{
return|return
name|String
operator|.
name|format
argument_list|(
literal|"%s(aborted:%s)"
argument_list|,
name|driverState
argument_list|,
name|aborted
operator|.
name|get
argument_list|()
argument_list|)
return|;
block|}
block|}
specifier|private
name|boolean
name|checkConcurrency
parameter_list|()
block|{
name|boolean
name|supportConcurrency
init|=
name|conf
operator|.
name|getBoolVar
argument_list|(
name|HiveConf
operator|.
name|ConfVars
operator|.
name|HIVE_SUPPORT_CONCURRENCY
argument_list|)
decl_stmt|;
if|if
condition|(
operator|!
name|supportConcurrency
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Concurrency mode is disabled, not creating a lock manager"
argument_list|)
expr_stmt|;
return|return
literal|false
return|;
block|}
return|return
literal|true
return|;
block|}
annotation|@
name|Override
specifier|public
name|Schema
name|getSchema
parameter_list|()
block|{
return|return
name|schema
return|;
block|}
specifier|public
name|Schema
name|getExplainSchema
parameter_list|()
block|{
return|return
operator|new
name|Schema
argument_list|(
name|ExplainTask
operator|.
name|getResultSchema
argument_list|()
argument_list|,
literal|null
argument_list|)
return|;
block|}
annotation|@
name|Override
specifier|public
name|Context
name|getContext
parameter_list|()
block|{
return|return
name|ctx
return|;
block|}
specifier|public
name|PlanMapper
name|getPlanMapper
parameter_list|()
block|{
return|return
name|ctx
operator|.
name|getPlanMapper
argument_list|()
return|;
block|}
comment|/**    * Get a Schema with fields represented with native Hive types    */
specifier|private
specifier|static
name|Schema
name|getSchema
parameter_list|(
name|BaseSemanticAnalyzer
name|sem
parameter_list|,
name|HiveConf
name|conf
parameter_list|)
block|{
name|Schema
name|schema
init|=
literal|null
decl_stmt|;
comment|// If we have a plan, prefer its logical result schema if it's
comment|// available; otherwise, try digging out a fetch task; failing that,
comment|// give up.
if|if
condition|(
name|sem
operator|==
literal|null
condition|)
block|{
comment|// can't get any info without a plan
block|}
elseif|else
if|if
condition|(
name|sem
operator|.
name|getResultSchema
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|List
argument_list|<
name|FieldSchema
argument_list|>
name|lst
init|=
name|sem
operator|.
name|getResultSchema
argument_list|()
decl_stmt|;
name|schema
operator|=
operator|new
name|Schema
argument_list|(
name|lst
argument_list|,
literal|null
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|sem
operator|.
name|getFetchTask
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|FetchTask
name|ft
init|=
name|sem
operator|.
name|getFetchTask
argument_list|()
decl_stmt|;
name|TableDesc
name|td
init|=
name|ft
operator|.
name|getTblDesc
argument_list|()
decl_stmt|;
comment|// partitioned tables don't have tableDesc set on the FetchTask. Instead
comment|// they have a list of PartitionDesc objects, each with a table desc.
comment|// Let's
comment|// try to fetch the desc for the first partition and use it's
comment|// deserializer.
if|if
condition|(
name|td
operator|==
literal|null
operator|&&
name|ft
operator|.
name|getWork
argument_list|()
operator|!=
literal|null
operator|&&
name|ft
operator|.
name|getWork
argument_list|()
operator|.
name|getPartDesc
argument_list|()
operator|!=
literal|null
condition|)
block|{
if|if
condition|(
name|ft
operator|.
name|getWork
argument_list|()
operator|.
name|getPartDesc
argument_list|()
operator|.
name|size
argument_list|()
operator|>
literal|0
condition|)
block|{
name|td
operator|=
name|ft
operator|.
name|getWork
argument_list|()
operator|.
name|getPartDesc
argument_list|()
operator|.
name|get
argument_list|(
literal|0
argument_list|)
operator|.
name|getTableDesc
argument_list|()
expr_stmt|;
block|}
block|}
if|if
condition|(
name|td
operator|==
literal|null
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"No returning schema."
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|String
name|tableName
init|=
literal|"result"
decl_stmt|;
name|List
argument_list|<
name|FieldSchema
argument_list|>
name|lst
init|=
literal|null
decl_stmt|;
try|try
block|{
name|lst
operator|=
name|HiveMetaStoreUtils
operator|.
name|getFieldsFromDeserializer
argument_list|(
name|tableName
argument_list|,
name|td
operator|.
name|getDeserializer
argument_list|(
name|conf
argument_list|)
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Error getting schema: "
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|lst
operator|!=
literal|null
condition|)
block|{
name|schema
operator|=
operator|new
name|Schema
argument_list|(
name|lst
argument_list|,
literal|null
argument_list|)
expr_stmt|;
block|}
block|}
block|}
if|if
condition|(
name|schema
operator|==
literal|null
condition|)
block|{
name|schema
operator|=
operator|new
name|Schema
argument_list|()
expr_stmt|;
block|}
name|LOG
operator|.
name|info
argument_list|(
literal|"Returning Hive schema: "
operator|+
name|schema
argument_list|)
expr_stmt|;
return|return
name|schema
return|;
block|}
comment|/**    * Set the maximum number of rows returned by getResults    */
annotation|@
name|Override
specifier|public
name|void
name|setMaxRows
parameter_list|(
name|int
name|maxRows
parameter_list|)
block|{
name|this
operator|.
name|maxRows
operator|=
name|maxRows
expr_stmt|;
block|}
annotation|@
name|VisibleForTesting
specifier|public
name|Driver
parameter_list|(
name|HiveConf
name|conf
parameter_list|)
block|{
name|this
argument_list|(
operator|new
name|QueryState
operator|.
name|Builder
argument_list|()
operator|.
name|withGenerateNewQueryId
argument_list|(
literal|true
argument_list|)
operator|.
name|withHiveConf
argument_list|(
name|conf
argument_list|)
operator|.
name|build
argument_list|()
argument_list|,
literal|null
argument_list|)
expr_stmt|;
block|}
comment|// Pass lineageState when a driver instantiates another Driver to run
comment|// or compile another query
specifier|public
name|Driver
parameter_list|(
name|HiveConf
name|conf
parameter_list|,
name|Context
name|ctx
parameter_list|,
name|LineageState
name|lineageState
parameter_list|)
block|{
name|this
argument_list|(
name|getNewQueryState
argument_list|(
name|conf
argument_list|,
name|lineageState
argument_list|)
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|)
expr_stmt|;
name|this
operator|.
name|ctx
operator|=
name|ctx
expr_stmt|;
block|}
comment|// Pass lineageState when a driver instantiates another Driver to run
comment|// or compile another query
specifier|public
name|Driver
parameter_list|(
name|HiveConf
name|conf
parameter_list|,
name|String
name|userName
parameter_list|,
name|LineageState
name|lineageState
parameter_list|)
block|{
name|this
argument_list|(
name|getNewQueryState
argument_list|(
name|conf
argument_list|,
name|lineageState
argument_list|)
argument_list|,
name|userName
argument_list|,
literal|null
argument_list|)
expr_stmt|;
block|}
specifier|public
name|Driver
parameter_list|(
name|QueryState
name|queryState
parameter_list|,
name|String
name|userName
parameter_list|)
block|{
name|this
argument_list|(
name|queryState
argument_list|,
name|userName
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|)
expr_stmt|;
block|}
specifier|public
name|Driver
parameter_list|(
name|QueryState
name|queryState
parameter_list|,
name|String
name|userName
parameter_list|,
name|QueryInfo
name|queryInfo
parameter_list|)
block|{
name|this
argument_list|(
name|queryState
argument_list|,
name|userName
argument_list|,
name|queryInfo
argument_list|,
literal|null
argument_list|)
expr_stmt|;
block|}
specifier|public
name|Driver
parameter_list|(
name|QueryState
name|queryState
parameter_list|,
name|String
name|userName
parameter_list|,
name|QueryInfo
name|queryInfo
parameter_list|,
name|HiveTxnManager
name|txnMgr
parameter_list|)
block|{
name|this
operator|.
name|queryState
operator|=
name|queryState
expr_stmt|;
name|this
operator|.
name|conf
operator|=
name|queryState
operator|.
name|getConf
argument_list|()
expr_stmt|;
name|this
operator|.
name|userName
operator|=
name|userName
expr_stmt|;
name|this
operator|.
name|hookRunner
operator|=
operator|new
name|HookRunner
argument_list|(
name|conf
argument_list|,
name|console
argument_list|)
expr_stmt|;
name|this
operator|.
name|queryInfo
operator|=
name|queryInfo
expr_stmt|;
name|this
operator|.
name|initTxnMgr
operator|=
name|txnMgr
expr_stmt|;
block|}
comment|/**    * Generating the new QueryState object. Making sure, that the new queryId is generated.    * @param conf The HiveConf which should be used    * @param lineageState a LineageState to be set in the new QueryState object    * @return The new QueryState object    */
specifier|private
specifier|static
name|QueryState
name|getNewQueryState
parameter_list|(
name|HiveConf
name|conf
parameter_list|,
name|LineageState
name|lineageState
parameter_list|)
block|{
return|return
operator|new
name|QueryState
operator|.
name|Builder
argument_list|()
operator|.
name|withGenerateNewQueryId
argument_list|(
literal|true
argument_list|)
operator|.
name|withHiveConf
argument_list|(
name|conf
argument_list|)
operator|.
name|withLineageState
argument_list|(
name|lineageState
argument_list|)
operator|.
name|build
argument_list|()
return|;
block|}
comment|/**    * Compile a new query, but potentially reset taskID counter.  Not resetting task counter    * is useful for generating re-entrant QL queries.    * @param command  The HiveQL query to compile    * @param resetTaskIds Resets taskID counter if true.    * @return 0 for ok    */
specifier|public
name|int
name|compile
parameter_list|(
name|String
name|command
parameter_list|,
name|boolean
name|resetTaskIds
parameter_list|)
block|{
try|try
block|{
name|compile
argument_list|(
name|command
argument_list|,
name|resetTaskIds
argument_list|,
literal|false
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
block|{
return|return
name|cpr
operator|.
name|getErrorCode
argument_list|()
return|;
block|}
block|}
comment|// deferClose indicates if the close/destroy should be deferred when the process has been
comment|// interrupted, it should be set to true if the compile is called within another method like
comment|// runInternal, which defers the close to the called in that method.
annotation|@
name|VisibleForTesting
name|void
name|compile
parameter_list|(
name|String
name|command
parameter_list|,
name|boolean
name|resetTaskIds
parameter_list|,
name|boolean
name|deferClose
parameter_list|)
throws|throws
name|CommandProcessorResponse
block|{
name|PerfLogger
name|perfLogger
init|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|()
decl_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|COMPILE
argument_list|)
expr_stmt|;
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|DriverState
operator|.
name|COMPILING
expr_stmt|;
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
name|command
operator|=
operator|new
name|VariableSubstitution
argument_list|(
operator|new
name|HiveVariableSource
argument_list|()
block|{
annotation|@
name|Override
specifier|public
name|Map
argument_list|<
name|String
argument_list|,
name|String
argument_list|>
name|getHiveVariable
parameter_list|()
block|{
return|return
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveVariables
argument_list|()
return|;
block|}
block|}
argument_list|)
operator|.
name|substitute
argument_list|(
name|conf
argument_list|,
name|command
argument_list|)
expr_stmt|;
name|String
name|queryStr
init|=
name|command
decl_stmt|;
try|try
block|{
comment|// command should be redacted to avoid to logging sensitive data
name|queryStr
operator|=
name|HookUtils
operator|.
name|redactLogString
argument_list|(
name|conf
argument_list|,
name|command
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"WARNING! Query command could not be redacted."
operator|+
name|e
argument_list|)
expr_stmt|;
block|}
name|checkInterrupted
argument_list|(
literal|"at beginning of compilation."
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|)
expr_stmt|;
if|if
condition|(
name|ctx
operator|!=
literal|null
operator|&&
name|ctx
operator|.
name|getExplainAnalyze
argument_list|()
operator|!=
name|AnalyzeState
operator|.
name|RUNNING
condition|)
block|{
comment|// close the existing ctx etc before compiling a new query, but does not destroy driver
name|closeInProcess
argument_list|(
literal|false
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|resetTaskIds
condition|)
block|{
name|TaskFactory
operator|.
name|resetId
argument_list|()
expr_stmt|;
block|}
name|LockedDriverState
operator|.
name|setLockedDriverState
argument_list|(
name|lDrvState
argument_list|)
expr_stmt|;
specifier|final
name|String
name|queryId
init|=
name|Strings
operator|.
name|isNullOrEmpty
argument_list|(
name|queryState
operator|.
name|getQueryId
argument_list|()
argument_list|)
condition|?
name|QueryPlan
operator|.
name|makeQueryId
argument_list|()
else|:
name|queryState
operator|.
name|getQueryId
argument_list|()
decl_stmt|;
name|SparkSession
name|ss
init|=
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getSparkSession
argument_list|()
decl_stmt|;
if|if
condition|(
name|ss
operator|!=
literal|null
condition|)
block|{
name|ss
operator|.
name|onQuerySubmission
argument_list|(
name|queryId
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|ctx
operator|!=
literal|null
condition|)
block|{
name|setTriggerContext
argument_list|(
name|queryId
argument_list|)
expr_stmt|;
block|}
comment|//save some info for webUI for use after plan is freed
name|this
operator|.
name|queryDisplay
operator|.
name|setQueryStr
argument_list|(
name|queryStr
argument_list|)
expr_stmt|;
name|this
operator|.
name|queryDisplay
operator|.
name|setQueryId
argument_list|(
name|queryId
argument_list|)
expr_stmt|;
name|LOG
operator|.
name|info
argument_list|(
literal|"Compiling command(queryId="
operator|+
name|queryId
operator|+
literal|"): "
operator|+
name|queryStr
argument_list|)
expr_stmt|;
name|conf
operator|.
name|setQueryString
argument_list|(
name|queryStr
argument_list|)
expr_stmt|;
comment|// FIXME: sideeffect will leave the last query set at the session level
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getConf
argument_list|()
operator|.
name|setQueryString
argument_list|(
name|queryStr
argument_list|)
expr_stmt|;
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|setupQueryCurrentTimestamp
argument_list|()
expr_stmt|;
block|}
comment|// Whether any error occurred during query compilation. Used for query lifetime hook.
name|boolean
name|compileError
init|=
literal|false
decl_stmt|;
name|boolean
name|parseError
init|=
literal|false
decl_stmt|;
try|try
block|{
comment|// Initialize the transaction manager.  This must be done before analyze is called.
if|if
condition|(
name|initTxnMgr
operator|!=
literal|null
condition|)
block|{
name|queryTxnMgr
operator|=
name|initTxnMgr
expr_stmt|;
block|}
else|else
block|{
name|queryTxnMgr
operator|=
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|initTxnMgr
argument_list|(
name|conf
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|queryTxnMgr
operator|instanceof
name|Configurable
condition|)
block|{
operator|(
operator|(
name|Configurable
operator|)
name|queryTxnMgr
operator|)
operator|.
name|setConf
argument_list|(
name|conf
argument_list|)
expr_stmt|;
block|}
name|queryState
operator|.
name|setTxnManager
argument_list|(
name|queryTxnMgr
argument_list|)
expr_stmt|;
comment|// In case when user Ctrl-C twice to kill Hive CLI JVM, we want to release locks
comment|// if compile is being called multiple times, clear the old shutdownhook
name|ShutdownHookManager
operator|.
name|removeShutdownHook
argument_list|(
name|shutdownRunner
argument_list|)
expr_stmt|;
specifier|final
name|HiveTxnManager
name|txnMgr
init|=
name|queryTxnMgr
decl_stmt|;
name|shutdownRunner
operator|=
operator|new
name|Runnable
argument_list|()
block|{
annotation|@
name|Override
specifier|public
name|void
name|run
parameter_list|()
block|{
try|try
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|false
argument_list|,
name|txnMgr
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Exception when releasing locks in ShutdownHook for Driver: "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
block|}
block|}
block|}
expr_stmt|;
name|ShutdownHookManager
operator|.
name|addShutdownHook
argument_list|(
name|shutdownRunner
argument_list|,
name|SHUTDOWN_HOOK_PRIORITY
argument_list|)
expr_stmt|;
name|checkInterrupted
argument_list|(
literal|"before parsing and analysing the query"
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|)
expr_stmt|;
if|if
condition|(
name|ctx
operator|==
literal|null
condition|)
block|{
name|ctx
operator|=
operator|new
name|Context
argument_list|(
name|conf
argument_list|)
expr_stmt|;
name|setTriggerContext
argument_list|(
name|queryId
argument_list|)
expr_stmt|;
block|}
name|ctx
operator|.
name|setHiveTxnManager
argument_list|(
name|queryTxnMgr
argument_list|)
expr_stmt|;
name|ctx
operator|.
name|setStatsSource
argument_list|(
name|statsSource
argument_list|)
expr_stmt|;
name|ctx
operator|.
name|setCmd
argument_list|(
name|command
argument_list|)
expr_stmt|;
name|ctx
operator|.
name|setHDFSCleanup
argument_list|(
literal|true
argument_list|)
expr_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|PARSE
argument_list|)
expr_stmt|;
comment|// Trigger query hook before compilation
name|hookRunner
operator|.
name|runBeforeParseHook
argument_list|(
name|command
argument_list|)
expr_stmt|;
name|ASTNode
name|tree
decl_stmt|;
try|try
block|{
name|tree
operator|=
name|ParseUtils
operator|.
name|parse
argument_list|(
name|command
argument_list|,
name|ctx
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|ParseException
name|e
parameter_list|)
block|{
name|parseError
operator|=
literal|true
expr_stmt|;
throw|throw
name|e
throw|;
block|}
finally|finally
block|{
name|hookRunner
operator|.
name|runAfterParseHook
argument_list|(
name|command
argument_list|,
name|parseError
argument_list|)
expr_stmt|;
block|}
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|PARSE
argument_list|)
expr_stmt|;
name|hookRunner
operator|.
name|runBeforeCompileHook
argument_list|(
name|command
argument_list|)
expr_stmt|;
comment|// clear CurrentFunctionsInUse set, to capture new set of functions
comment|// that SemanticAnalyzer finds are in use
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getCurrentFunctionsInUse
argument_list|()
operator|.
name|clear
argument_list|()
expr_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|ANALYZE
argument_list|)
expr_stmt|;
comment|// Flush the metastore cache.  This assures that we don't pick up objects from a previous
comment|// query running in this same thread.  This has to be done after we get our semantic
comment|// analyzer (this is when the connection to the metastore is made) but before we analyze,
comment|// because at that point we need access to the objects.
name|Hive
operator|.
name|get
argument_list|()
operator|.
name|getMSC
argument_list|()
operator|.
name|flushCache
argument_list|()
expr_stmt|;
name|backupContext
operator|=
operator|new
name|Context
argument_list|(
name|ctx
argument_list|)
expr_stmt|;
name|boolean
name|executeHooks
init|=
name|hookRunner
operator|.
name|hasPreAnalyzeHooks
argument_list|()
decl_stmt|;
name|HiveSemanticAnalyzerHookContext
name|hookCtx
init|=
operator|new
name|HiveSemanticAnalyzerHookContextImpl
argument_list|()
decl_stmt|;
if|if
condition|(
name|executeHooks
condition|)
block|{
name|hookCtx
operator|.
name|setConf
argument_list|(
name|conf
argument_list|)
expr_stmt|;
name|hookCtx
operator|.
name|setUserName
argument_list|(
name|userName
argument_list|)
expr_stmt|;
name|hookCtx
operator|.
name|setIpAddress
argument_list|(
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getUserIpAddress
argument_list|()
argument_list|)
expr_stmt|;
name|hookCtx
operator|.
name|setCommand
argument_list|(
name|command
argument_list|)
expr_stmt|;
name|hookCtx
operator|.
name|setHiveOperation
argument_list|(
name|queryState
operator|.
name|getHiveOperation
argument_list|()
argument_list|)
expr_stmt|;
name|tree
operator|=
name|hookRunner
operator|.
name|runPreAnalyzeHooks
argument_list|(
name|hookCtx
argument_list|,
name|tree
argument_list|)
expr_stmt|;
block|}
comment|// Do semantic analysis and plan generation
name|BaseSemanticAnalyzer
name|sem
init|=
name|SemanticAnalyzerFactory
operator|.
name|get
argument_list|(
name|queryState
argument_list|,
name|tree
argument_list|)
decl_stmt|;
if|if
condition|(
operator|!
name|retrial
condition|)
block|{
if|if
condition|(
operator|(
name|queryState
operator|.
name|getHiveOperation
argument_list|()
operator|!=
literal|null
operator|)
operator|&&
name|queryState
operator|.
name|getHiveOperation
argument_list|()
operator|.
name|equals
argument_list|(
name|HiveOperation
operator|.
name|REPLDUMP
argument_list|)
condition|)
block|{
name|setLastReplIdForDump
argument_list|(
name|queryState
operator|.
name|getConf
argument_list|()
argument_list|)
expr_stmt|;
block|}
name|openTransaction
argument_list|()
expr_stmt|;
name|generateValidTxnList
argument_list|()
expr_stmt|;
block|}
name|sem
operator|.
name|analyze
argument_list|(
name|tree
argument_list|,
name|ctx
argument_list|)
expr_stmt|;
if|if
condition|(
name|executeHooks
condition|)
block|{
name|hookCtx
operator|.
name|update
argument_list|(
name|sem
argument_list|)
expr_stmt|;
name|hookRunner
operator|.
name|runPostAnalyzeHooks
argument_list|(
name|hookCtx
argument_list|,
name|sem
operator|.
name|getAllRootTasks
argument_list|()
argument_list|)
expr_stmt|;
block|}
name|LOG
operator|.
name|info
argument_list|(
literal|"Semantic Analysis Completed (retrial = {})"
argument_list|,
name|retrial
argument_list|)
expr_stmt|;
comment|// Retrieve information about cache usage for the query.
if|if
condition|(
name|conf
operator|.
name|getBoolVar
argument_list|(
name|HiveConf
operator|.
name|ConfVars
operator|.
name|HIVE_QUERY_RESULTS_CACHE_ENABLED
argument_list|)
condition|)
block|{
name|cacheUsage
operator|=
name|sem
operator|.
name|getCacheUsage
argument_list|()
expr_stmt|;
block|}
comment|// validate the plan
name|sem
operator|.
name|validate
argument_list|()
expr_stmt|;
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|ANALYZE
argument_list|)
expr_stmt|;
name|checkInterrupted
argument_list|(
literal|"after analyzing query."
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|)
expr_stmt|;
comment|// get the output schema
name|schema
operator|=
name|getSchema
argument_list|(
name|sem
argument_list|,
name|conf
argument_list|)
expr_stmt|;
name|plan
operator|=
operator|new
name|QueryPlan
argument_list|(
name|queryStr
argument_list|,
name|sem
argument_list|,
name|queryDisplay
operator|.
name|getQueryStartTime
argument_list|()
argument_list|,
name|queryId
argument_list|,
name|queryState
operator|.
name|getHiveOperation
argument_list|()
argument_list|,
name|schema
argument_list|)
expr_stmt|;
comment|// save the optimized plan and sql for the explain
name|plan
operator|.
name|setOptimizedCBOPlan
argument_list|(
name|ctx
operator|.
name|getCalcitePlan
argument_list|()
argument_list|)
expr_stmt|;
name|plan
operator|.
name|setOptimizedQueryString
argument_list|(
name|ctx
operator|.
name|getOptimizedSql
argument_list|()
argument_list|)
expr_stmt|;
name|conf
operator|.
name|set
argument_list|(
literal|"mapreduce.workflow.id"
argument_list|,
literal|"hive_"
operator|+
name|queryId
argument_list|)
expr_stmt|;
name|conf
operator|.
name|set
argument_list|(
literal|"mapreduce.workflow.name"
argument_list|,
name|queryStr
argument_list|)
expr_stmt|;
comment|// initialize FetchTask right here
if|if
condition|(
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|.
name|initialize
argument_list|(
name|queryState
argument_list|,
name|plan
argument_list|,
literal|null
argument_list|,
name|ctx
operator|.
name|getOpContext
argument_list|()
argument_list|)
expr_stmt|;
block|}
comment|//do the authorization check
if|if
condition|(
operator|!
name|sem
operator|.
name|skipAuthorization
argument_list|()
operator|&&
name|HiveConf
operator|.
name|getBoolVar
argument_list|(
name|conf
argument_list|,
name|HiveConf
operator|.
name|ConfVars
operator|.
name|HIVE_AUTHORIZATION_ENABLED
argument_list|)
condition|)
block|{
try|try
block|{
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|DO_AUTHORIZATION
argument_list|)
expr_stmt|;
comment|// Authorization check for kill query will be in KillQueryImpl
comment|// As both admin or operation owner can perform the operation.
comment|// Which is not directly supported in authorizer
if|if
condition|(
name|queryState
operator|.
name|getHiveOperation
argument_list|()
operator|!=
name|HiveOperation
operator|.
name|KILL_QUERY
condition|)
block|{
name|CommandAuthorizer
operator|.
name|doAuthorization
argument_list|(
name|queryState
operator|.
name|getHiveOperation
argument_list|()
argument_list|,
name|sem
argument_list|,
name|command
argument_list|)
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|AuthorizationException
name|authExp
parameter_list|)
block|{
name|console
operator|.
name|printError
argument_list|(
literal|"Authorization failed:"
operator|+
name|authExp
operator|.
name|getMessage
argument_list|()
operator|+
literal|". Use SHOW GRANT to get more details."
argument_list|)
expr_stmt|;
name|errorMessage
operator|=
name|authExp
operator|.
name|getMessage
argument_list|()
expr_stmt|;
name|SQLState
operator|=
literal|"42000"
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|403
argument_list|)
throw|;
block|}
finally|finally
block|{
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|DO_AUTHORIZATION
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|conf
operator|.
name|getBoolVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_LOG_EXPLAIN_OUTPUT
argument_list|)
operator|||
name|conf
operator|.
name|getBoolVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_SERVER2_WEBUI_EXPLAIN_OUTPUT
argument_list|)
condition|)
block|{
name|String
name|explainOutput
init|=
name|getExplainOutput
argument_list|(
name|sem
argument_list|,
name|plan
argument_list|,
name|tree
argument_list|)
decl_stmt|;
if|if
condition|(
name|explainOutput
operator|!=
literal|null
condition|)
block|{
if|if
condition|(
name|conf
operator|.
name|getBoolVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_LOG_EXPLAIN_OUTPUT
argument_list|)
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"EXPLAIN output for queryid "
operator|+
name|queryId
operator|+
literal|" : "
operator|+
name|explainOutput
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|conf
operator|.
name|isWebUiQueryInfoCacheEnabled
argument_list|()
operator|&&
name|conf
operator|.
name|getBoolVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_SERVER2_WEBUI_EXPLAIN_OUTPUT
argument_list|)
condition|)
block|{
name|queryDisplay
operator|.
name|setExplainPlan
argument_list|(
name|explainOutput
argument_list|)
expr_stmt|;
block|}
block|}
block|}
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
block|{
throw|throw
name|cpr
throw|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|checkInterrupted
argument_list|(
literal|"during query compilation: "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|)
expr_stmt|;
name|compileError
operator|=
literal|true
expr_stmt|;
name|ErrorMsg
name|error
init|=
name|ErrorMsg
operator|.
name|getErrorMsg
argument_list|(
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
decl_stmt|;
name|errorMessage
operator|=
literal|"FAILED: "
operator|+
name|e
operator|.
name|getClass
argument_list|()
operator|.
name|getSimpleName
argument_list|()
expr_stmt|;
if|if
condition|(
name|error
operator|!=
name|ErrorMsg
operator|.
name|GENERIC_ERROR
condition|)
block|{
name|errorMessage
operator|+=
literal|" [Error "
operator|+
name|error
operator|.
name|getErrorCode
argument_list|()
operator|+
literal|"]:"
expr_stmt|;
block|}
comment|// HIVE-4889
if|if
condition|(
operator|(
name|e
operator|instanceof
name|IllegalArgumentException
operator|)
operator|&&
name|e
operator|.
name|getMessage
argument_list|()
operator|==
literal|null
operator|&&
name|e
operator|.
name|getCause
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|errorMessage
operator|+=
literal|" "
operator|+
name|e
operator|.
name|getCause
argument_list|()
operator|.
name|getMessage
argument_list|()
expr_stmt|;
block|}
else|else
block|{
name|errorMessage
operator|+=
literal|" "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|error
operator|==
name|ErrorMsg
operator|.
name|TXNMGR_NOT_ACID
condition|)
block|{
name|errorMessage
operator|+=
literal|". Failed command: "
operator|+
name|queryStr
expr_stmt|;
block|}
name|SQLState
operator|=
name|error
operator|.
name|getSQLState
argument_list|()
expr_stmt|;
name|downstreamError
operator|=
name|e
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|,
literal|"\n"
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
name|error
operator|.
name|getErrorCode
argument_list|()
argument_list|)
throw|;
block|}
finally|finally
block|{
comment|// Trigger post compilation hook. Note that if the compilation fails here then
comment|// before/after execution hook will never be executed.
if|if
condition|(
operator|!
name|parseError
condition|)
block|{
try|try
block|{
name|hookRunner
operator|.
name|runAfterCompilationHook
argument_list|(
name|command
argument_list|,
name|compileError
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Failed when invoking query after-compilation hook."
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
block|}
name|double
name|duration
init|=
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|COMPILE
argument_list|)
operator|/
literal|1000.00
decl_stmt|;
name|ImmutableMap
argument_list|<
name|String
argument_list|,
name|Long
argument_list|>
name|compileHMSTimings
init|=
name|dumpMetaCallTimingWithoutEx
argument_list|(
literal|"compilation"
argument_list|)
decl_stmt|;
name|queryDisplay
operator|.
name|setHmsTimings
argument_list|(
name|QueryDisplay
operator|.
name|Phase
operator|.
name|COMPILATION
argument_list|,
name|compileHMSTimings
argument_list|)
expr_stmt|;
name|boolean
name|isInterrupted
init|=
name|lDrvState
operator|.
name|isAborted
argument_list|()
decl_stmt|;
if|if
condition|(
name|isInterrupted
operator|&&
operator|!
name|deferClose
condition|)
block|{
name|closeInProcess
argument_list|(
literal|true
argument_list|)
expr_stmt|;
block|}
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
if|if
condition|(
name|isInterrupted
condition|)
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|deferClose
condition|?
name|DriverState
operator|.
name|EXECUTING
else|:
name|DriverState
operator|.
name|ERROR
expr_stmt|;
block|}
else|else
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|compileError
condition|?
name|DriverState
operator|.
name|ERROR
else|:
name|DriverState
operator|.
name|COMPILED
expr_stmt|;
block|}
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|isInterrupted
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Compiling command(queryId="
operator|+
name|queryId
operator|+
literal|") has been interrupted after "
operator|+
name|duration
operator|+
literal|" seconds"
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Completed compiling command(queryId="
operator|+
name|queryId
operator|+
literal|"); Time taken: "
operator|+
name|duration
operator|+
literal|" seconds"
argument_list|)
expr_stmt|;
block|}
block|}
block|}
comment|// Checks whether txn list has been invalidated while planning the query.
comment|// This would happen if query requires exclusive/semi-shared lock, and there
comment|// has been a committed transaction on the table over which the lock is
comment|// required.
specifier|private
name|boolean
name|isValidTxnListState
parameter_list|()
throws|throws
name|LockException
block|{
comment|// 1) Get valid txn list.
name|String
name|txnString
init|=
name|conf
operator|.
name|get
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|)
decl_stmt|;
if|if
condition|(
name|txnString
operator|==
literal|null
condition|)
block|{
comment|// Not a transactional op, nothing more to do
return|return
literal|true
return|;
block|}
name|ValidTxnList
name|currentTxnList
init|=
name|queryTxnMgr
operator|.
name|getValidTxns
argument_list|()
decl_stmt|;
name|String
name|currentTxnString
init|=
name|currentTxnList
operator|.
name|toString
argument_list|()
decl_stmt|;
if|if
condition|(
name|currentTxnString
operator|.
name|equals
argument_list|(
name|txnString
argument_list|)
condition|)
block|{
comment|// Still valid, nothing more to do
return|return
literal|true
return|;
block|}
comment|// 2) Get locks that are relevant:
comment|// - Exclusive for INSERT OVERWRITE.
comment|// - Semi-shared for UPDATE/DELETE.
if|if
condition|(
name|ctx
operator|.
name|getHiveLocks
argument_list|()
operator|==
literal|null
operator|||
name|ctx
operator|.
name|getHiveLocks
argument_list|()
operator|.
name|isEmpty
argument_list|()
condition|)
block|{
comment|// Nothing to check
return|return
literal|true
return|;
block|}
name|Set
argument_list|<
name|String
argument_list|>
name|nonSharedLocks
init|=
operator|new
name|HashSet
argument_list|<>
argument_list|()
decl_stmt|;
for|for
control|(
name|HiveLock
name|lock
range|:
name|ctx
operator|.
name|getHiveLocks
argument_list|()
control|)
block|{
if|if
condition|(
name|lock
operator|.
name|mayContainComponents
argument_list|()
condition|)
block|{
comment|// The lock may have multiple components, e.g., DbHiveLock, hence we need
comment|// to check for each of them
for|for
control|(
name|LockComponent
name|lckCmp
range|:
name|lock
operator|.
name|getHiveLockComponents
argument_list|()
control|)
block|{
comment|// We only consider tables for which we hold either an exclusive
comment|// or a shared write lock
if|if
condition|(
operator|(
name|lckCmp
operator|.
name|getType
argument_list|()
operator|==
name|LockType
operator|.
name|EXCLUSIVE
operator|||
name|lckCmp
operator|.
name|getType
argument_list|()
operator|==
name|LockType
operator|.
name|SHARED_WRITE
operator|)
operator|&&
name|lckCmp
operator|.
name|getTablename
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|nonSharedLocks
operator|.
name|add
argument_list|(
name|Warehouse
operator|.
name|getQualifiedName
argument_list|(
name|lckCmp
operator|.
name|getDbname
argument_list|()
argument_list|,
name|lckCmp
operator|.
name|getTablename
argument_list|()
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
block|}
else|else
block|{
comment|// The lock has a single components, e.g., SimpleHiveLock or ZooKeeperHiveLock.
comment|// Pos 0 of lock paths array contains dbname, pos 1 contains tblname
if|if
condition|(
operator|(
name|lock
operator|.
name|getHiveLockMode
argument_list|()
operator|==
name|HiveLockMode
operator|.
name|EXCLUSIVE
operator|||
name|lock
operator|.
name|getHiveLockMode
argument_list|()
operator|==
name|HiveLockMode
operator|.
name|SEMI_SHARED
operator|)
operator|&&
name|lock
operator|.
name|getHiveLockObject
argument_list|()
operator|.
name|getPaths
argument_list|()
operator|.
name|length
operator|==
literal|2
condition|)
block|{
name|nonSharedLocks
operator|.
name|add
argument_list|(
name|Warehouse
operator|.
name|getQualifiedName
argument_list|(
name|lock
operator|.
name|getHiveLockObject
argument_list|()
operator|.
name|getPaths
argument_list|()
index|[
literal|0
index|]
argument_list|,
name|lock
operator|.
name|getHiveLockObject
argument_list|()
operator|.
name|getPaths
argument_list|()
index|[
literal|1
index|]
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
block|}
comment|// 3) Get txn tables that are being written
name|String
name|txnWriteIdListStr
init|=
name|conf
operator|.
name|get
argument_list|(
name|ValidTxnWriteIdList
operator|.
name|VALID_TABLES_WRITEIDS_KEY
argument_list|)
decl_stmt|;
if|if
condition|(
name|txnWriteIdListStr
operator|==
literal|null
operator|||
name|txnWriteIdListStr
operator|.
name|length
argument_list|()
operator|==
literal|0
condition|)
block|{
comment|// Nothing to check
return|return
literal|true
return|;
block|}
name|ValidTxnWriteIdList
name|txnWriteIdList
init|=
operator|new
name|ValidTxnWriteIdList
argument_list|(
name|txnWriteIdListStr
argument_list|)
decl_stmt|;
name|List
argument_list|<
name|Pair
argument_list|<
name|String
argument_list|,
name|Table
argument_list|>
argument_list|>
name|writtenTables
init|=
name|getWrittenTableList
argument_list|(
name|plan
argument_list|)
decl_stmt|;
name|ValidTxnWriteIdList
name|currentTxnWriteIds
init|=
name|queryTxnMgr
operator|.
name|getValidWriteIds
argument_list|(
name|writtenTables
operator|.
name|stream
argument_list|()
operator|.
name|filter
argument_list|(
name|e
lambda|->
name|AcidUtils
operator|.
name|isTransactionalTable
argument_list|(
name|e
operator|.
name|getRight
argument_list|()
argument_list|)
argument_list|)
operator|.
name|map
argument_list|(
name|e
lambda|->
name|e
operator|.
name|getLeft
argument_list|()
argument_list|)
operator|.
name|collect
argument_list|(
name|Collectors
operator|.
name|toList
argument_list|()
argument_list|)
argument_list|,
name|currentTxnString
argument_list|)
decl_stmt|;
for|for
control|(
name|Pair
argument_list|<
name|String
argument_list|,
name|Table
argument_list|>
name|tableInfo
range|:
name|writtenTables
control|)
block|{
name|String
name|fullQNameForLock
init|=
name|Warehouse
operator|.
name|getQualifiedName
argument_list|(
name|tableInfo
operator|.
name|getRight
argument_list|()
operator|.
name|getDbName
argument_list|()
argument_list|,
name|MetaStoreUtils
operator|.
name|encodeTableName
argument_list|(
name|tableInfo
operator|.
name|getRight
argument_list|()
operator|.
name|getTableName
argument_list|()
argument_list|)
argument_list|)
decl_stmt|;
if|if
condition|(
name|nonSharedLocks
operator|.
name|contains
argument_list|(
name|fullQNameForLock
argument_list|)
condition|)
block|{
comment|// Check if table is transactional
if|if
condition|(
name|AcidUtils
operator|.
name|isTransactionalTable
argument_list|(
name|tableInfo
operator|.
name|getRight
argument_list|()
argument_list|)
condition|)
block|{
comment|// Check that write id is still valid
if|if
condition|(
operator|!
name|TxnIdUtils
operator|.
name|checkEquivalentWriteIds
argument_list|(
name|txnWriteIdList
operator|.
name|getTableValidWriteIdList
argument_list|(
name|tableInfo
operator|.
name|getLeft
argument_list|()
argument_list|)
argument_list|,
name|currentTxnWriteIds
operator|.
name|getTableValidWriteIdList
argument_list|(
name|tableInfo
operator|.
name|getLeft
argument_list|()
argument_list|)
argument_list|)
condition|)
block|{
comment|// Write id has changed, it is not valid anymore,
comment|// we need to recompile
return|return
literal|false
return|;
block|}
block|}
name|nonSharedLocks
operator|.
name|remove
argument_list|(
name|fullQNameForLock
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
operator|!
name|nonSharedLocks
operator|.
name|isEmpty
argument_list|()
condition|)
block|{
throw|throw
operator|new
name|LockException
argument_list|(
literal|"Wrong state: non-shared locks contain information for tables that have not"
operator|+
literal|" been visited when trying to validate the locks from query tables.\n"
operator|+
literal|"Tables: "
operator|+
name|writtenTables
operator|.
name|stream
argument_list|()
operator|.
name|map
argument_list|(
name|e
lambda|->
name|e
operator|.
name|getLeft
argument_list|()
argument_list|)
operator|.
name|collect
argument_list|(
name|Collectors
operator|.
name|toList
argument_list|()
argument_list|)
operator|+
literal|"\n"
operator|+
literal|"Remaining locks after check: "
operator|+
name|nonSharedLocks
argument_list|)
throw|;
block|}
comment|// It passes the test, it is valid
return|return
literal|true
return|;
block|}
specifier|private
name|void
name|setTriggerContext
parameter_list|(
specifier|final
name|String
name|queryId
parameter_list|)
block|{
specifier|final
name|long
name|queryStartTime
decl_stmt|;
comment|// query info is created by SQLOperation which will have start time of the operation. When JDBC Statement is not
comment|// used queryInfo will be null, in which case we take creation of Driver instance as query start time (which is also
comment|// the time when query display object is created)
if|if
condition|(
name|queryInfo
operator|!=
literal|null
condition|)
block|{
name|queryStartTime
operator|=
name|queryInfo
operator|.
name|getBeginTime
argument_list|()
expr_stmt|;
block|}
else|else
block|{
name|queryStartTime
operator|=
name|queryDisplay
operator|.
name|getQueryStartTime
argument_list|()
expr_stmt|;
block|}
name|WmContext
name|wmContext
init|=
operator|new
name|WmContext
argument_list|(
name|queryStartTime
argument_list|,
name|queryId
argument_list|)
decl_stmt|;
name|ctx
operator|.
name|setWmContext
argument_list|(
name|wmContext
argument_list|)
expr_stmt|;
block|}
comment|/**    * Last repl id should be captured before opening txn by current REPL DUMP operation.    * This is needed to avoid losing data which are added/modified by concurrent txns when bootstrap    * dump in progress.    * @param conf Query configurations    * @throws HiveException    * @throws TException    */
specifier|private
name|void
name|setLastReplIdForDump
parameter_list|(
name|HiveConf
name|conf
parameter_list|)
throws|throws
name|HiveException
throws|,
name|TException
block|{
comment|// Last logged notification event id would be the last repl Id for the current REPl DUMP.
name|Hive
name|hiveDb
init|=
name|Hive
operator|.
name|get
argument_list|()
decl_stmt|;
name|Long
name|lastReplId
init|=
name|hiveDb
operator|.
name|getMSC
argument_list|()
operator|.
name|getCurrentNotificationEventId
argument_list|()
operator|.
name|getEventId
argument_list|()
decl_stmt|;
name|conf
operator|.
name|setLong
argument_list|(
name|ReplUtils
operator|.
name|LAST_REPL_ID_KEY
argument_list|,
name|lastReplId
argument_list|)
expr_stmt|;
name|LOG
operator|.
name|debug
argument_list|(
literal|"Setting "
operator|+
name|ReplUtils
operator|.
name|LAST_REPL_ID_KEY
operator|+
literal|" = "
operator|+
name|lastReplId
argument_list|)
expr_stmt|;
block|}
specifier|private
name|void
name|openTransaction
parameter_list|()
throws|throws
name|LockException
throws|,
name|CommandProcessorResponse
block|{
if|if
condition|(
name|checkConcurrency
argument_list|()
operator|&&
name|startImplicitTxn
argument_list|(
name|queryTxnMgr
argument_list|)
condition|)
block|{
name|String
name|userFromUGI
init|=
name|getUserFromUGI
argument_list|()
decl_stmt|;
if|if
condition|(
operator|!
name|queryTxnMgr
operator|.
name|isTxnOpen
argument_list|()
condition|)
block|{
if|if
condition|(
name|userFromUGI
operator|==
literal|null
condition|)
block|{
throw|throw
name|createProcessorResponse
argument_list|(
literal|10
argument_list|)
throw|;
block|}
name|queryTxnMgr
operator|.
name|openTxn
argument_list|(
name|ctx
argument_list|,
name|userFromUGI
argument_list|)
expr_stmt|;
block|}
block|}
block|}
specifier|private
name|void
name|generateValidTxnList
parameter_list|()
throws|throws
name|LockException
block|{
comment|// Record current valid txn list that will be used throughout the query
comment|// compilation and processing. We only do this if 1) a transaction
comment|// was already opened and 2) the list has not been recorded yet,
comment|// e.g., by an explicit open transaction command.
name|validTxnListsGenerated
operator|=
literal|false
expr_stmt|;
name|String
name|currentTxnString
init|=
name|conf
operator|.
name|get
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|)
decl_stmt|;
if|if
condition|(
name|queryTxnMgr
operator|.
name|isTxnOpen
argument_list|()
operator|&&
operator|(
name|currentTxnString
operator|==
literal|null
operator|||
name|currentTxnString
operator|.
name|isEmpty
argument_list|()
operator|)
condition|)
block|{
try|try
block|{
name|recordValidTxns
argument_list|(
name|queryTxnMgr
argument_list|)
expr_stmt|;
name|validTxnListsGenerated
operator|=
literal|true
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|error
argument_list|(
literal|"Exception while acquiring valid txn list"
argument_list|,
name|e
argument_list|)
expr_stmt|;
throw|throw
name|e
throw|;
block|}
block|}
block|}
specifier|private
name|boolean
name|startImplicitTxn
parameter_list|(
name|HiveTxnManager
name|txnManager
parameter_list|)
throws|throws
name|LockException
block|{
name|boolean
name|shouldOpenImplicitTxn
init|=
operator|!
name|ctx
operator|.
name|isExplainPlan
argument_list|()
decl_stmt|;
comment|//this is dumb. HiveOperation is not always set. see HIVE-16447/HIVE-16443
switch|switch
condition|(
name|queryState
operator|.
name|getHiveOperation
argument_list|()
operator|==
literal|null
condition|?
name|HiveOperation
operator|.
name|QUERY
else|:
name|queryState
operator|.
name|getHiveOperation
argument_list|()
condition|)
block|{
case|case
name|COMMIT
case|:
case|case
name|ROLLBACK
case|:
if|if
condition|(
operator|!
name|txnManager
operator|.
name|isTxnOpen
argument_list|()
condition|)
block|{
throw|throw
operator|new
name|LockException
argument_list|(
literal|null
argument_list|,
name|ErrorMsg
operator|.
name|OP_NOT_ALLOWED_WITHOUT_TXN
argument_list|,
name|queryState
operator|.
name|getHiveOperation
argument_list|()
operator|.
name|getOperationName
argument_list|()
argument_list|)
throw|;
block|}
case|case
name|SWITCHDATABASE
case|:
case|case
name|SET_AUTOCOMMIT
case|:
comment|/**          * autocommit is here for completeness.  TM doesn't use it.  If we want to support JDBC          * semantics (or any other definition of autocommit) it should be done at session level.          */
case|case
name|SHOWDATABASES
case|:
case|case
name|SHOWTABLES
case|:
case|case
name|SHOWCOLUMNS
case|:
case|case
name|SHOWFUNCTIONS
case|:
case|case
name|SHOWPARTITIONS
case|:
case|case
name|SHOWLOCKS
case|:
case|case
name|SHOWVIEWS
case|:
case|case
name|SHOW_ROLES
case|:
case|case
name|SHOW_ROLE_PRINCIPALS
case|:
case|case
name|SHOW_COMPACTIONS
case|:
case|case
name|SHOW_TRANSACTIONS
case|:
case|case
name|ABORT_TRANSACTIONS
case|:
case|case
name|KILL_QUERY
case|:
name|shouldOpenImplicitTxn
operator|=
literal|false
expr_stmt|;
comment|//this implies that no locks are needed for such a command
block|}
return|return
name|shouldOpenImplicitTxn
return|;
block|}
specifier|private
name|int
name|handleInterruptionWithHook
parameter_list|(
name|String
name|msg
parameter_list|,
name|HookContext
name|hookContext
parameter_list|,
name|PerfLogger
name|perfLogger
parameter_list|)
block|{
name|SQLState
operator|=
literal|"HY008"
expr_stmt|;
comment|//SQLState for cancel operation
name|errorMessage
operator|=
literal|"FAILED: command has been interrupted: "
operator|+
name|msg
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
if|if
condition|(
name|hookContext
operator|!=
literal|null
condition|)
block|{
try|try
block|{
name|invokeFailureHooks
argument_list|(
name|perfLogger
argument_list|,
name|hookContext
argument_list|,
name|errorMessage
argument_list|,
literal|null
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Caught exception attempting to invoke Failure Hooks"
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
block|}
return|return
literal|1000
return|;
block|}
specifier|private
name|void
name|checkInterrupted
parameter_list|(
name|String
name|msg
parameter_list|,
name|HookContext
name|hookContext
parameter_list|,
name|PerfLogger
name|perfLogger
parameter_list|)
throws|throws
name|CommandProcessorResponse
block|{
if|if
condition|(
name|lDrvState
operator|.
name|isAborted
argument_list|()
condition|)
block|{
throw|throw
name|createProcessorResponse
argument_list|(
name|handleInterruptionWithHook
argument_list|(
name|msg
argument_list|,
name|hookContext
argument_list|,
name|perfLogger
argument_list|)
argument_list|)
throw|;
block|}
block|}
specifier|private
name|ImmutableMap
argument_list|<
name|String
argument_list|,
name|Long
argument_list|>
name|dumpMetaCallTimingWithoutEx
parameter_list|(
name|String
name|phase
parameter_list|)
block|{
try|try
block|{
return|return
name|Hive
operator|.
name|get
argument_list|()
operator|.
name|dumpAndClearMetaCallTiming
argument_list|(
name|phase
argument_list|)
return|;
block|}
catch|catch
parameter_list|(
name|HiveException
name|he
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Caught exception attempting to write metadata call information "
operator|+
name|he
argument_list|,
name|he
argument_list|)
expr_stmt|;
block|}
return|return
literal|null
return|;
block|}
comment|/**    * Returns EXPLAIN EXTENDED output for a semantically    * analyzed query.    *    * @param sem semantic analyzer for analyzed query    * @param plan query plan    * @param astTree AST tree dump    * @throws java.io.IOException    */
specifier|private
name|String
name|getExplainOutput
parameter_list|(
name|BaseSemanticAnalyzer
name|sem
parameter_list|,
name|QueryPlan
name|plan
parameter_list|,
name|ASTNode
name|astTree
parameter_list|)
throws|throws
name|IOException
block|{
name|String
name|ret
init|=
literal|null
decl_stmt|;
name|ExplainTask
name|task
init|=
operator|new
name|ExplainTask
argument_list|()
decl_stmt|;
name|task
operator|.
name|initialize
argument_list|(
name|queryState
argument_list|,
name|plan
argument_list|,
literal|null
argument_list|,
name|ctx
operator|.
name|getOpContext
argument_list|()
argument_list|)
expr_stmt|;
name|ByteArrayOutputStream
name|baos
init|=
operator|new
name|ByteArrayOutputStream
argument_list|()
decl_stmt|;
name|PrintStream
name|ps
init|=
operator|new
name|PrintStream
argument_list|(
name|baos
argument_list|)
decl_stmt|;
try|try
block|{
name|List
argument_list|<
name|Task
argument_list|<
name|?
argument_list|>
argument_list|>
name|rootTasks
init|=
name|sem
operator|.
name|getAllRootTasks
argument_list|()
decl_stmt|;
if|if
condition|(
name|conf
operator|.
name|getBoolVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_SERVER2_WEBUI_SHOW_GRAPH
argument_list|)
condition|)
block|{
name|JSONObject
name|jsonPlan
init|=
name|task
operator|.
name|getJSONPlan
argument_list|(
literal|null
argument_list|,
name|rootTasks
argument_list|,
name|sem
operator|.
name|getFetchTask
argument_list|()
argument_list|,
literal|true
argument_list|,
literal|true
argument_list|,
literal|true
argument_list|,
name|sem
operator|.
name|getCboInfo
argument_list|()
argument_list|,
name|plan
operator|.
name|getOptimizedCBOPlan
argument_list|()
argument_list|,
name|plan
operator|.
name|getOptimizedQueryString
argument_list|()
argument_list|)
decl_stmt|;
if|if
condition|(
name|jsonPlan
operator|.
name|getJSONObject
argument_list|(
name|ExplainTask
operator|.
name|STAGE_DEPENDENCIES
argument_list|)
operator|!=
literal|null
operator|&&
name|jsonPlan
operator|.
name|getJSONObject
argument_list|(
name|ExplainTask
operator|.
name|STAGE_DEPENDENCIES
argument_list|)
operator|.
name|length
argument_list|()
operator|<=
name|conf
operator|.
name|getIntVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_SERVER2_WEBUI_MAX_GRAPH_SIZE
argument_list|)
condition|)
block|{
name|ret
operator|=
name|jsonPlan
operator|.
name|toString
argument_list|()
expr_stmt|;
block|}
else|else
block|{
name|ret
operator|=
literal|null
expr_stmt|;
block|}
block|}
else|else
block|{
name|task
operator|.
name|getJSONPlan
argument_list|(
name|ps
argument_list|,
name|rootTasks
argument_list|,
name|sem
operator|.
name|getFetchTask
argument_list|()
argument_list|,
literal|false
argument_list|,
literal|true
argument_list|,
literal|true
argument_list|,
name|sem
operator|.
name|getCboInfo
argument_list|()
argument_list|,
name|plan
operator|.
name|getOptimizedCBOPlan
argument_list|()
argument_list|,
name|plan
operator|.
name|getOptimizedQueryString
argument_list|()
argument_list|)
expr_stmt|;
name|ret
operator|=
name|baos
operator|.
name|toString
argument_list|()
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Exception generating explain output: "
operator|+
name|e
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
return|return
name|ret
return|;
block|}
annotation|@
name|Override
specifier|public
name|HiveConf
name|getConf
parameter_list|()
block|{
return|return
name|conf
return|;
block|}
comment|/**    * @return The current query plan associated with this Driver, if any.    */
annotation|@
name|Override
specifier|public
name|QueryPlan
name|getPlan
parameter_list|()
block|{
return|return
name|plan
return|;
block|}
comment|/**    * @return The current FetchTask associated with the Driver's plan, if any.    */
annotation|@
name|Override
specifier|public
name|FetchTask
name|getFetchTask
parameter_list|()
block|{
return|return
name|fetchTask
return|;
block|}
comment|// Write the current set of valid transactions into the conf file
specifier|private
name|void
name|recordValidTxns
parameter_list|(
name|HiveTxnManager
name|txnMgr
parameter_list|)
throws|throws
name|LockException
block|{
name|String
name|oldTxnString
init|=
name|conf
operator|.
name|get
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|)
decl_stmt|;
if|if
condition|(
operator|(
name|oldTxnString
operator|!=
literal|null
operator|)
operator|&&
operator|(
name|oldTxnString
operator|.
name|length
argument_list|()
operator|>
literal|0
operator|)
condition|)
block|{
throw|throw
operator|new
name|IllegalStateException
argument_list|(
literal|"calling recordValidTxn() more than once in the same "
operator|+
name|JavaUtils
operator|.
name|txnIdToString
argument_list|(
name|txnMgr
operator|.
name|getCurrentTxnId
argument_list|()
argument_list|)
argument_list|)
throw|;
block|}
name|ValidTxnList
name|txnList
init|=
name|txnMgr
operator|.
name|getValidTxns
argument_list|()
decl_stmt|;
name|String
name|txnStr
init|=
name|txnList
operator|.
name|toString
argument_list|()
decl_stmt|;
name|conf
operator|.
name|set
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|,
name|txnStr
argument_list|)
expr_stmt|;
name|LOG
operator|.
name|debug
argument_list|(
literal|"Encoding valid txns info "
operator|+
name|txnStr
operator|+
literal|" txnid:"
operator|+
name|txnMgr
operator|.
name|getCurrentTxnId
argument_list|()
argument_list|)
expr_stmt|;
block|}
comment|// Write the current set of valid write ids for the operated acid tables into the conf file so
comment|// that it can be read by the input format.
specifier|private
name|ValidTxnWriteIdList
name|recordValidWriteIds
parameter_list|(
name|HiveTxnManager
name|txnMgr
parameter_list|)
throws|throws
name|LockException
block|{
name|String
name|txnString
init|=
name|conf
operator|.
name|get
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|)
decl_stmt|;
if|if
condition|(
operator|(
name|txnString
operator|==
literal|null
operator|)
operator|||
operator|(
name|txnString
operator|.
name|isEmpty
argument_list|()
operator|)
condition|)
block|{
throw|throw
operator|new
name|IllegalStateException
argument_list|(
literal|"calling recordValidWritsIdss() without initializing ValidTxnList "
operator|+
name|JavaUtils
operator|.
name|txnIdToString
argument_list|(
name|txnMgr
operator|.
name|getCurrentTxnId
argument_list|()
argument_list|)
argument_list|)
throw|;
block|}
name|List
argument_list|<
name|String
argument_list|>
name|txnTables
init|=
name|getTransactionalTableList
argument_list|(
name|plan
argument_list|)
decl_stmt|;
name|ValidTxnWriteIdList
name|txnWriteIds
init|=
literal|null
decl_stmt|;
if|if
condition|(
name|compactionWriteIds
operator|!=
literal|null
condition|)
block|{
comment|/**        * This is kludgy: here we need to read with Compactor's snapshot/txn        * rather than the snapshot of the current {@code txnMgr}, in effect        * simulating a "flashback query" but can't actually share compactor's        * txn since it would run multiple statements.  See more comments in        * {@link org.apache.hadoop.hive.ql.txn.compactor.Worker} where it start        * the compactor txn*/
if|if
condition|(
name|txnTables
operator|.
name|size
argument_list|()
operator|!=
literal|1
condition|)
block|{
throw|throw
operator|new
name|LockException
argument_list|(
literal|"Unexpected tables in compaction: "
operator|+
name|txnTables
argument_list|)
throw|;
block|}
name|String
name|fullTableName
init|=
name|txnTables
operator|.
name|get
argument_list|(
literal|0
argument_list|)
decl_stmt|;
name|txnWriteIds
operator|=
operator|new
name|ValidTxnWriteIdList
argument_list|(
name|compactorTxnId
argument_list|)
expr_stmt|;
name|txnWriteIds
operator|.
name|addTableValidWriteIdList
argument_list|(
name|compactionWriteIds
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|txnWriteIds
operator|=
name|txnMgr
operator|.
name|getValidWriteIds
argument_list|(
name|txnTables
argument_list|,
name|txnString
argument_list|)
expr_stmt|;
block|}
name|String
name|writeIdStr
init|=
name|txnWriteIds
operator|.
name|toString
argument_list|()
decl_stmt|;
name|conf
operator|.
name|set
argument_list|(
name|ValidTxnWriteIdList
operator|.
name|VALID_TABLES_WRITEIDS_KEY
argument_list|,
name|writeIdStr
argument_list|)
expr_stmt|;
if|if
condition|(
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|!=
literal|null
condition|)
block|{
comment|/**        * This is needed for {@link HiveConf.ConfVars.HIVEFETCHTASKCONVERSION} optimization which        * initializes JobConf in FetchOperator before recordValidTxns() but this has to be done        * after locks are acquired to avoid race conditions in ACID.        * This case is supported only for single source query.        */
name|Operator
argument_list|<
name|?
argument_list|>
name|source
init|=
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|.
name|getWork
argument_list|()
operator|.
name|getSource
argument_list|()
decl_stmt|;
if|if
condition|(
name|source
operator|instanceof
name|TableScanOperator
condition|)
block|{
name|TableScanOperator
name|tsOp
init|=
operator|(
name|TableScanOperator
operator|)
name|source
decl_stmt|;
name|String
name|fullTableName
init|=
name|AcidUtils
operator|.
name|getFullTableName
argument_list|(
name|tsOp
operator|.
name|getConf
argument_list|()
operator|.
name|getDatabaseName
argument_list|()
argument_list|,
name|tsOp
operator|.
name|getConf
argument_list|()
operator|.
name|getTableName
argument_list|()
argument_list|)
decl_stmt|;
name|ValidWriteIdList
name|writeIdList
init|=
name|txnWriteIds
operator|.
name|getTableValidWriteIdList
argument_list|(
name|fullTableName
argument_list|)
decl_stmt|;
if|if
condition|(
name|tsOp
operator|.
name|getConf
argument_list|()
operator|.
name|isTranscationalTable
argument_list|()
operator|&&
operator|(
name|writeIdList
operator|==
literal|null
operator|)
condition|)
block|{
throw|throw
operator|new
name|IllegalStateException
argument_list|(
literal|"ACID table: "
operator|+
name|fullTableName
operator|+
literal|" is missing from the ValidWriteIdList config: "
operator|+
name|writeIdStr
argument_list|)
throw|;
block|}
if|if
condition|(
name|writeIdList
operator|!=
literal|null
condition|)
block|{
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|.
name|setValidWriteIdList
argument_list|(
name|writeIdList
operator|.
name|toString
argument_list|()
argument_list|)
expr_stmt|;
block|}
block|}
block|}
name|LOG
operator|.
name|debug
argument_list|(
literal|"Encoding valid txn write ids info "
operator|+
name|writeIdStr
operator|+
literal|" txnid:"
operator|+
name|txnMgr
operator|.
name|getCurrentTxnId
argument_list|()
argument_list|)
expr_stmt|;
return|return
name|txnWriteIds
return|;
block|}
comment|// Make the list of transactional tables list which are getting read or written by current txn
specifier|private
name|List
argument_list|<
name|String
argument_list|>
name|getTransactionalTableList
parameter_list|(
name|QueryPlan
name|plan
parameter_list|)
block|{
name|Set
argument_list|<
name|String
argument_list|>
name|tableList
init|=
operator|new
name|HashSet
argument_list|<>
argument_list|()
decl_stmt|;
for|for
control|(
name|ReadEntity
name|input
range|:
name|plan
operator|.
name|getInputs
argument_list|()
control|)
block|{
name|addTableFromEntity
argument_list|(
name|input
argument_list|,
name|tableList
argument_list|)
expr_stmt|;
block|}
for|for
control|(
name|WriteEntity
name|output
range|:
name|plan
operator|.
name|getOutputs
argument_list|()
control|)
block|{
name|addTableFromEntity
argument_list|(
name|output
argument_list|,
name|tableList
argument_list|)
expr_stmt|;
block|}
return|return
operator|new
name|ArrayList
argument_list|<
name|String
argument_list|>
argument_list|(
name|tableList
argument_list|)
return|;
block|}
specifier|private
name|void
name|addTableFromEntity
parameter_list|(
name|Entity
name|entity
parameter_list|,
name|Collection
argument_list|<
name|String
argument_list|>
name|tableList
parameter_list|)
block|{
name|Table
name|tbl
decl_stmt|;
switch|switch
condition|(
name|entity
operator|.
name|getType
argument_list|()
condition|)
block|{
case|case
name|TABLE
case|:
block|{
name|tbl
operator|=
name|entity
operator|.
name|getTable
argument_list|()
expr_stmt|;
break|break;
block|}
case|case
name|PARTITION
case|:
case|case
name|DUMMYPARTITION
case|:
block|{
name|tbl
operator|=
name|entity
operator|.
name|getPartition
argument_list|()
operator|.
name|getTable
argument_list|()
expr_stmt|;
break|break;
block|}
default|default:
block|{
return|return;
block|}
block|}
if|if
condition|(
operator|!
name|AcidUtils
operator|.
name|isTransactionalTable
argument_list|(
name|tbl
argument_list|)
condition|)
block|{
return|return;
block|}
name|String
name|fullTableName
init|=
name|AcidUtils
operator|.
name|getFullTableName
argument_list|(
name|tbl
operator|.
name|getDbName
argument_list|()
argument_list|,
name|tbl
operator|.
name|getTableName
argument_list|()
argument_list|)
decl_stmt|;
name|tableList
operator|.
name|add
argument_list|(
name|fullTableName
argument_list|)
expr_stmt|;
block|}
comment|// Make the list of transactional tables list which are getting written by current txn
specifier|private
name|List
argument_list|<
name|Pair
argument_list|<
name|String
argument_list|,
name|Table
argument_list|>
argument_list|>
name|getWrittenTableList
parameter_list|(
name|QueryPlan
name|plan
parameter_list|)
block|{
name|List
argument_list|<
name|Pair
argument_list|<
name|String
argument_list|,
name|Table
argument_list|>
argument_list|>
name|result
init|=
operator|new
name|ArrayList
argument_list|<>
argument_list|()
decl_stmt|;
name|Set
argument_list|<
name|String
argument_list|>
name|tableList
init|=
operator|new
name|HashSet
argument_list|<>
argument_list|()
decl_stmt|;
for|for
control|(
name|WriteEntity
name|output
range|:
name|plan
operator|.
name|getOutputs
argument_list|()
control|)
block|{
name|Table
name|tbl
decl_stmt|;
switch|switch
condition|(
name|output
operator|.
name|getType
argument_list|()
condition|)
block|{
case|case
name|TABLE
case|:
block|{
name|tbl
operator|=
name|output
operator|.
name|getTable
argument_list|()
expr_stmt|;
break|break;
block|}
case|case
name|PARTITION
case|:
case|case
name|DUMMYPARTITION
case|:
block|{
name|tbl
operator|=
name|output
operator|.
name|getPartition
argument_list|()
operator|.
name|getTable
argument_list|()
expr_stmt|;
break|break;
block|}
default|default:
block|{
continue|continue;
block|}
block|}
name|String
name|fullTableName
init|=
name|AcidUtils
operator|.
name|getFullTableName
argument_list|(
name|tbl
operator|.
name|getDbName
argument_list|()
argument_list|,
name|tbl
operator|.
name|getTableName
argument_list|()
argument_list|)
decl_stmt|;
if|if
condition|(
name|tableList
operator|.
name|add
argument_list|(
name|fullTableName
argument_list|)
condition|)
block|{
name|result
operator|.
name|add
argument_list|(
operator|new
name|ImmutablePair
argument_list|(
name|fullTableName
argument_list|,
name|tbl
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
return|return
name|result
return|;
block|}
specifier|private
name|String
name|getUserFromUGI
parameter_list|()
block|{
comment|// Don't use the userName member, as it may or may not have been set.  Get the value from
comment|// conf, which calls into getUGI to figure out who the process is running as.
try|try
block|{
return|return
name|conf
operator|.
name|getUser
argument_list|()
return|;
block|}
catch|catch
parameter_list|(
name|IOException
name|e
parameter_list|)
block|{
name|errorMessage
operator|=
literal|"FAILED: Error in determining user while acquiring locks: "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
expr_stmt|;
name|SQLState
operator|=
name|ErrorMsg
operator|.
name|findSQLState
argument_list|(
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
name|downstreamError
operator|=
name|e
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|,
literal|"\n"
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
block|}
return|return
literal|null
return|;
block|}
comment|/**    * Acquire read and write locks needed by the statement. The list of objects to be locked are    * obtained from the inputs and outputs populated by the compiler.  Locking strategy depends on    * HiveTxnManager and HiveLockManager configured    *    * This method also records the list of valid transactions.  This must be done after any    * transactions have been opened.    * @throws CommandProcessorResponse    **/
specifier|private
name|void
name|acquireLocks
parameter_list|()
throws|throws
name|CommandProcessorResponse
block|{
name|PerfLogger
name|perfLogger
init|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|()
decl_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|ACQUIRE_READ_WRITE_LOCKS
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|queryTxnMgr
operator|.
name|isTxnOpen
argument_list|()
operator|&&
name|queryTxnMgr
operator|.
name|supportsAcid
argument_list|()
condition|)
block|{
comment|/*non acid txn managers don't support txns but fwd lock requests to lock managers         acid txn manager requires all locks to be associated with a txn so if we         end up here w/o an open txn it's because we are processing something like "use<database>         which by definition needs no locks*/
return|return;
block|}
try|try
block|{
name|String
name|userFromUGI
init|=
name|getUserFromUGI
argument_list|()
decl_stmt|;
if|if
condition|(
name|userFromUGI
operator|==
literal|null
condition|)
block|{
throw|throw
name|createProcessorResponse
argument_list|(
literal|10
argument_list|)
throw|;
block|}
comment|// Set the table write id in all of the acid file sinks
if|if
condition|(
operator|!
name|plan
operator|.
name|getAcidSinks
argument_list|()
operator|.
name|isEmpty
argument_list|()
condition|)
block|{
name|List
argument_list|<
name|FileSinkDesc
argument_list|>
name|acidSinks
init|=
operator|new
name|ArrayList
argument_list|<>
argument_list|(
name|plan
operator|.
name|getAcidSinks
argument_list|()
argument_list|)
decl_stmt|;
comment|//sorting makes tests easier to write since file names and ROW__IDs depend on statementId
comment|//so this makes (file name -> data) mapping stable
name|acidSinks
operator|.
name|sort
argument_list|(
parameter_list|(
name|FileSinkDesc
name|fsd1
parameter_list|,
name|FileSinkDesc
name|fsd2
parameter_list|)
lambda|->
name|fsd1
operator|.
name|getDirName
argument_list|()
operator|.
name|compareTo
argument_list|(
name|fsd2
operator|.
name|getDirName
argument_list|()
argument_list|)
argument_list|)
expr_stmt|;
for|for
control|(
name|FileSinkDesc
name|desc
range|:
name|acidSinks
control|)
block|{
name|TableDesc
name|tableInfo
init|=
name|desc
operator|.
name|getTableInfo
argument_list|()
decl_stmt|;
name|long
name|writeId
init|=
name|queryTxnMgr
operator|.
name|getTableWriteId
argument_list|(
name|Utilities
operator|.
name|getDatabaseName
argument_list|(
name|tableInfo
operator|.
name|getTableName
argument_list|()
argument_list|)
argument_list|,
name|Utilities
operator|.
name|getTableName
argument_list|(
name|tableInfo
operator|.
name|getTableName
argument_list|()
argument_list|)
argument_list|)
decl_stmt|;
name|desc
operator|.
name|setTableWriteId
argument_list|(
name|writeId
argument_list|)
expr_stmt|;
comment|/**            * it's possible to have> 1 FileSink writing to the same table/partition            * e.g. Merge stmt, multi-insert stmt when mixing DP and SP writes            * Insert ... Select ... Union All Select ... using            * {@link org.apache.hadoop.hive.ql.exec.AbstractFileMergeOperator#UNION_SUDBIR_PREFIX}            */
name|desc
operator|.
name|setStatementId
argument_list|(
name|queryTxnMgr
operator|.
name|getStmtIdAndIncrement
argument_list|()
argument_list|)
expr_stmt|;
name|String
name|unionAllSubdir
init|=
literal|"/"
operator|+
name|AbstractFileMergeOperator
operator|.
name|UNION_SUDBIR_PREFIX
decl_stmt|;
if|if
condition|(
name|desc
operator|.
name|getInsertOverwrite
argument_list|()
operator|&&
name|desc
operator|.
name|getDirName
argument_list|()
operator|.
name|toString
argument_list|()
operator|.
name|contains
argument_list|(
name|unionAllSubdir
argument_list|)
operator|&&
name|desc
operator|.
name|isFullAcidTable
argument_list|()
condition|)
block|{
throw|throw
operator|new
name|UnsupportedOperationException
argument_list|(
literal|"QueryId="
operator|+
name|plan
operator|.
name|getQueryId
argument_list|()
operator|+
literal|" is not supported due to OVERWRITE and UNION ALL.  Please use truncate + insert"
argument_list|)
throw|;
block|}
block|}
block|}
if|if
condition|(
name|plan
operator|.
name|getAcidAnalyzeTable
argument_list|()
operator|!=
literal|null
condition|)
block|{
comment|// Allocate write ID for the table being analyzed.
name|Table
name|t
init|=
name|plan
operator|.
name|getAcidAnalyzeTable
argument_list|()
operator|.
name|getTable
argument_list|()
decl_stmt|;
name|queryTxnMgr
operator|.
name|getTableWriteId
argument_list|(
name|t
operator|.
name|getDbName
argument_list|()
argument_list|,
name|t
operator|.
name|getTableName
argument_list|()
argument_list|)
expr_stmt|;
block|}
name|DDLDescWithWriteId
name|acidDdlDesc
init|=
name|plan
operator|.
name|getAcidDdlDesc
argument_list|()
decl_stmt|;
name|boolean
name|hasAcidDdl
init|=
name|acidDdlDesc
operator|!=
literal|null
operator|&&
name|acidDdlDesc
operator|.
name|mayNeedWriteId
argument_list|()
decl_stmt|;
if|if
condition|(
name|hasAcidDdl
condition|)
block|{
name|String
name|fqTableName
init|=
name|acidDdlDesc
operator|.
name|getFullTableName
argument_list|()
decl_stmt|;
name|long
name|writeId
init|=
name|queryTxnMgr
operator|.
name|getTableWriteId
argument_list|(
name|Utilities
operator|.
name|getDatabaseName
argument_list|(
name|fqTableName
argument_list|)
argument_list|,
name|Utilities
operator|.
name|getTableName
argument_list|(
name|fqTableName
argument_list|)
argument_list|)
decl_stmt|;
name|acidDdlDesc
operator|.
name|setWriteId
argument_list|(
name|writeId
argument_list|)
expr_stmt|;
block|}
comment|/*It's imperative that {@code acquireLocks()} is called for all commands so that       HiveTxnManager can transition its state machine correctly*/
name|queryTxnMgr
operator|.
name|acquireLocks
argument_list|(
name|plan
argument_list|,
name|ctx
argument_list|,
name|userFromUGI
argument_list|,
name|lDrvState
argument_list|)
expr_stmt|;
specifier|final
name|List
argument_list|<
name|HiveLock
argument_list|>
name|locks
init|=
name|ctx
operator|.
name|getHiveLocks
argument_list|()
decl_stmt|;
name|LOG
operator|.
name|info
argument_list|(
literal|"Operation {} obtained {} locks"
argument_list|,
name|plan
operator|.
name|getOperation
argument_list|()
argument_list|,
operator|(
operator|(
name|locks
operator|==
literal|null
operator|)
condition|?
literal|0
else|:
name|locks
operator|.
name|size
argument_list|()
operator|)
argument_list|)
expr_stmt|;
comment|// This check is for controlling the correctness of the current state
if|if
condition|(
name|queryTxnMgr
operator|.
name|recordSnapshot
argument_list|(
name|plan
argument_list|)
operator|&&
operator|!
name|validTxnListsGenerated
condition|)
block|{
throw|throw
operator|new
name|IllegalStateException
argument_list|(
literal|"Need to record valid WriteID list but there is no valid TxnID list ("
operator|+
name|JavaUtils
operator|.
name|txnIdToString
argument_list|(
name|queryTxnMgr
operator|.
name|getCurrentTxnId
argument_list|()
argument_list|)
operator|+
literal|", queryId:"
operator|+
name|plan
operator|.
name|getQueryId
argument_list|()
operator|+
literal|")"
argument_list|)
throw|;
block|}
if|if
condition|(
name|plan
operator|.
name|hasAcidResourcesInQuery
argument_list|()
operator|||
name|hasAcidDdl
condition|)
block|{
name|recordValidWriteIds
argument_list|(
name|queryTxnMgr
argument_list|)
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|errorMessage
operator|=
literal|"FAILED: Error in acquiring locks: "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
expr_stmt|;
name|SQLState
operator|=
name|ErrorMsg
operator|.
name|findSQLState
argument_list|(
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
name|downstreamError
operator|=
name|e
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|,
literal|"\n"
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|10
argument_list|)
throw|;
block|}
finally|finally
block|{
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|ACQUIRE_READ_WRITE_LOCKS
argument_list|)
expr_stmt|;
block|}
block|}
specifier|public
name|void
name|releaseLocksAndCommitOrRollback
parameter_list|(
name|boolean
name|commit
parameter_list|)
throws|throws
name|LockException
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
name|commit
argument_list|,
name|queryTxnMgr
argument_list|)
expr_stmt|;
block|}
comment|/**    * @param commit if there is an open transaction and if true, commit,    *               if false rollback.  If there is no open transaction this parameter is ignored.    * @param txnManager an optional existing transaction manager retrieved earlier from the session    *    **/
annotation|@
name|VisibleForTesting
specifier|public
name|void
name|releaseLocksAndCommitOrRollback
parameter_list|(
name|boolean
name|commit
parameter_list|,
name|HiveTxnManager
name|txnManager
parameter_list|)
throws|throws
name|LockException
block|{
name|PerfLogger
name|perfLogger
init|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|()
decl_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|RELEASE_LOCKS
argument_list|)
expr_stmt|;
name|HiveTxnManager
name|txnMgr
decl_stmt|;
if|if
condition|(
name|txnManager
operator|==
literal|null
condition|)
block|{
comment|// Default to driver's txn manager if no txn manager specified
name|txnMgr
operator|=
name|queryTxnMgr
expr_stmt|;
block|}
else|else
block|{
name|txnMgr
operator|=
name|txnManager
expr_stmt|;
block|}
comment|// If we've opened a transaction we need to commit or rollback rather than explicitly
comment|// releasing the locks.
name|conf
operator|.
name|unset
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|)
expr_stmt|;
name|conf
operator|.
name|unset
argument_list|(
name|ValidTxnWriteIdList
operator|.
name|VALID_TABLES_WRITEIDS_KEY
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|checkConcurrency
argument_list|()
condition|)
block|{
return|return;
block|}
if|if
condition|(
name|txnMgr
operator|.
name|isTxnOpen
argument_list|()
condition|)
block|{
if|if
condition|(
name|commit
condition|)
block|{
if|if
condition|(
name|conf
operator|.
name|getBoolVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_IN_TEST
argument_list|)
operator|&&
name|conf
operator|.
name|getBoolVar
argument_list|(
name|ConfVars
operator|.
name|HIVETESTMODEROLLBACKTXN
argument_list|)
condition|)
block|{
name|txnMgr
operator|.
name|rollbackTxn
argument_list|()
expr_stmt|;
block|}
else|else
block|{
name|txnMgr
operator|.
name|commitTxn
argument_list|()
expr_stmt|;
comment|//both commit& rollback clear ALL locks for this tx
block|}
block|}
else|else
block|{
name|txnMgr
operator|.
name|rollbackTxn
argument_list|()
expr_stmt|;
block|}
block|}
else|else
block|{
comment|//since there is no tx, we only have locks for current query (if any)
if|if
condition|(
name|ctx
operator|!=
literal|null
operator|&&
name|ctx
operator|.
name|getHiveLocks
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|hiveLocks
operator|.
name|addAll
argument_list|(
name|ctx
operator|.
name|getHiveLocks
argument_list|()
argument_list|)
expr_stmt|;
block|}
name|txnMgr
operator|.
name|releaseLocks
argument_list|(
name|hiveLocks
argument_list|)
expr_stmt|;
block|}
name|hiveLocks
operator|.
name|clear
argument_list|()
expr_stmt|;
if|if
condition|(
name|ctx
operator|!=
literal|null
condition|)
block|{
name|ctx
operator|.
name|setHiveLocks
argument_list|(
literal|null
argument_list|)
expr_stmt|;
block|}
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|RELEASE_LOCKS
argument_list|)
expr_stmt|;
block|}
comment|/**    * Release some resources after a query is executed    * while keeping the result around.    */
specifier|public
name|void
name|releaseResources
parameter_list|()
block|{
name|releasePlan
argument_list|()
expr_stmt|;
name|releaseDriverContext
argument_list|()
expr_stmt|;
block|}
annotation|@
name|Override
specifier|public
name|CommandProcessorResponse
name|run
parameter_list|(
name|String
name|command
parameter_list|)
block|{
return|return
name|run
argument_list|(
name|command
argument_list|,
literal|false
argument_list|)
return|;
block|}
annotation|@
name|Override
specifier|public
name|CommandProcessorResponse
name|run
parameter_list|()
block|{
return|return
name|run
argument_list|(
literal|null
argument_list|,
literal|true
argument_list|)
return|;
block|}
specifier|public
name|CommandProcessorResponse
name|run
parameter_list|(
name|String
name|command
parameter_list|,
name|boolean
name|alreadyCompiled
parameter_list|)
block|{
try|try
block|{
name|runInternal
argument_list|(
name|command
argument_list|,
name|alreadyCompiled
argument_list|)
expr_stmt|;
return|return
name|createProcessorResponse
argument_list|(
literal|0
argument_list|)
return|;
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
block|{
name|SessionState
name|ss
init|=
name|SessionState
operator|.
name|get
argument_list|()
decl_stmt|;
if|if
condition|(
name|ss
operator|==
literal|null
condition|)
block|{
return|return
name|cpr
return|;
block|}
name|MetaDataFormatter
name|mdf
init|=
name|MetaDataFormatUtils
operator|.
name|getFormatter
argument_list|(
name|ss
operator|.
name|getConf
argument_list|()
argument_list|)
decl_stmt|;
if|if
condition|(
operator|!
operator|(
name|mdf
operator|instanceof
name|JsonMetaDataFormatter
operator|)
condition|)
block|{
return|return
name|cpr
return|;
block|}
comment|/*Here we want to encode the error in machine readable way (e.g. JSON)      * Ideally, errorCode would always be set to a canonical error defined in ErrorMsg.      * In practice that is rarely the case, so the messy logic below tries to tease      * out canonical error code if it can.  Exclude stack trace from output when      * the error is a specific/expected one.      * It's written to stdout for backward compatibility (WebHCat consumes it).*/
try|try
block|{
if|if
condition|(
name|downstreamError
operator|==
literal|null
condition|)
block|{
name|mdf
operator|.
name|error
argument_list|(
name|ss
operator|.
name|out
argument_list|,
name|errorMessage
argument_list|,
name|cpr
operator|.
name|getResponseCode
argument_list|()
argument_list|,
name|SQLState
argument_list|)
expr_stmt|;
return|return
name|cpr
return|;
block|}
name|ErrorMsg
name|canonicalErr
init|=
name|ErrorMsg
operator|.
name|getErrorMsg
argument_list|(
name|cpr
operator|.
name|getResponseCode
argument_list|()
argument_list|)
decl_stmt|;
if|if
condition|(
name|canonicalErr
operator|!=
literal|null
operator|&&
name|canonicalErr
operator|!=
name|ErrorMsg
operator|.
name|GENERIC_ERROR
condition|)
block|{
comment|/*Some HiveExceptions (e.g. SemanticException) don't set           canonical ErrorMsg explicitly, but there is logic           (e.g. #compile()) to find an appropriate canonical error and           return its code as error code. In this case we want to           preserve it for downstream code to interpret*/
name|mdf
operator|.
name|error
argument_list|(
name|ss
operator|.
name|out
argument_list|,
name|errorMessage
argument_list|,
name|cpr
operator|.
name|getResponseCode
argument_list|()
argument_list|,
name|SQLState
argument_list|,
literal|null
argument_list|)
expr_stmt|;
return|return
name|cpr
return|;
block|}
if|if
condition|(
name|downstreamError
operator|instanceof
name|HiveException
condition|)
block|{
name|HiveException
name|rc
init|=
operator|(
name|HiveException
operator|)
name|downstreamError
decl_stmt|;
name|mdf
operator|.
name|error
argument_list|(
name|ss
operator|.
name|out
argument_list|,
name|errorMessage
argument_list|,
name|rc
operator|.
name|getCanonicalErrorMsg
argument_list|()
operator|.
name|getErrorCode
argument_list|()
argument_list|,
name|SQLState
argument_list|,
name|rc
operator|.
name|getCanonicalErrorMsg
argument_list|()
operator|==
name|ErrorMsg
operator|.
name|GENERIC_ERROR
condition|?
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|rc
argument_list|)
else|:
literal|null
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|ErrorMsg
name|canonicalMsg
init|=
name|ErrorMsg
operator|.
name|getErrorMsg
argument_list|(
name|downstreamError
operator|.
name|getMessage
argument_list|()
argument_list|)
decl_stmt|;
name|mdf
operator|.
name|error
argument_list|(
name|ss
operator|.
name|out
argument_list|,
name|errorMessage
argument_list|,
name|canonicalMsg
operator|.
name|getErrorCode
argument_list|()
argument_list|,
name|SQLState
argument_list|,
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|downstreamError
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|HiveException
name|ex
parameter_list|)
block|{
name|console
operator|.
name|printError
argument_list|(
literal|"Unable to JSON-encode the error"
argument_list|,
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|ex
argument_list|)
argument_list|)
expr_stmt|;
block|}
return|return
name|cpr
return|;
block|}
block|}
annotation|@
name|Override
specifier|public
name|CommandProcessorResponse
name|compileAndRespond
parameter_list|(
name|String
name|command
parameter_list|)
block|{
return|return
name|compileAndRespond
argument_list|(
name|command
argument_list|,
literal|false
argument_list|)
return|;
block|}
specifier|public
name|CommandProcessorResponse
name|compileAndRespond
parameter_list|(
name|String
name|command
parameter_list|,
name|boolean
name|cleanupTxnList
parameter_list|)
block|{
try|try
block|{
name|compileInternal
argument_list|(
name|command
argument_list|,
literal|false
argument_list|)
expr_stmt|;
return|return
name|createProcessorResponse
argument_list|(
literal|0
argument_list|)
return|;
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|e
parameter_list|)
block|{
return|return
name|e
return|;
block|}
finally|finally
block|{
if|if
condition|(
name|cleanupTxnList
condition|)
block|{
comment|// Valid txn list might be generated for a query compiled using this
comment|// command, thus we need to reset it
name|conf
operator|.
name|unset
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|)
expr_stmt|;
block|}
block|}
block|}
specifier|public
name|void
name|lockAndRespond
parameter_list|()
throws|throws
name|CommandProcessorResponse
block|{
comment|// Assumes the query has already been compiled
if|if
condition|(
name|plan
operator|==
literal|null
condition|)
block|{
throw|throw
operator|new
name|IllegalStateException
argument_list|(
literal|"No previously compiled query for driver - queryId="
operator|+
name|queryState
operator|.
name|getQueryId
argument_list|()
argument_list|)
throw|;
block|}
if|if
condition|(
name|requiresLock
argument_list|()
condition|)
block|{
try|try
block|{
name|acquireLocks
argument_list|()
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
block|{
name|rollback
argument_list|(
name|cpr
argument_list|)
expr_stmt|;
throw|throw
name|cpr
throw|;
block|}
block|}
block|}
specifier|private
name|void
name|compileInternal
parameter_list|(
name|String
name|command
parameter_list|,
name|boolean
name|deferClose
parameter_list|)
throws|throws
name|CommandProcessorResponse
block|{
name|Metrics
name|metrics
init|=
name|MetricsFactory
operator|.
name|getInstance
argument_list|()
decl_stmt|;
if|if
condition|(
name|metrics
operator|!=
literal|null
condition|)
block|{
name|metrics
operator|.
name|incrementCounter
argument_list|(
name|MetricsConstant
operator|.
name|WAITING_COMPILE_OPS
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
name|PerfLogger
name|perfLogger
init|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|(
literal|true
argument_list|)
decl_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|WAIT_COMPILE
argument_list|)
expr_stmt|;
try|try
init|(
name|CompileLock
name|compileLock
init|=
name|CompileLockFactory
operator|.
name|newInstance
argument_list|(
name|conf
argument_list|,
name|command
argument_list|)
init|)
block|{
name|boolean
name|success
init|=
name|compileLock
operator|.
name|tryAcquire
argument_list|()
decl_stmt|;
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|WAIT_COMPILE
argument_list|)
expr_stmt|;
if|if
condition|(
name|metrics
operator|!=
literal|null
condition|)
block|{
name|metrics
operator|.
name|decrementCounter
argument_list|(
name|MetricsConstant
operator|.
name|WAITING_COMPILE_OPS
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|success
condition|)
block|{
name|errorMessage
operator|=
name|ErrorMsg
operator|.
name|COMPILE_LOCK_TIMED_OUT
operator|.
name|getErrorCodedMsg
argument_list|()
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
name|ErrorMsg
operator|.
name|COMPILE_LOCK_TIMED_OUT
operator|.
name|getErrorCode
argument_list|()
argument_list|)
throw|;
block|}
try|try
block|{
name|compile
argument_list|(
name|command
argument_list|,
literal|true
argument_list|,
name|deferClose
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
block|{
try|try
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|false
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Exception in releasing locks. "
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
block|}
throw|throw
name|cpr
throw|;
block|}
block|}
comment|//Save compile-time PerfLogging for WebUI.
comment|//Execution-time Perf logs are done by either another thread's PerfLogger
comment|//or a reset PerfLogger.
name|queryDisplay
operator|.
name|setPerfLogStarts
argument_list|(
name|QueryDisplay
operator|.
name|Phase
operator|.
name|COMPILATION
argument_list|,
name|perfLogger
operator|.
name|getStartTimes
argument_list|()
argument_list|)
expr_stmt|;
name|queryDisplay
operator|.
name|setPerfLogEnds
argument_list|(
name|QueryDisplay
operator|.
name|Phase
operator|.
name|COMPILATION
argument_list|,
name|perfLogger
operator|.
name|getEndTimes
argument_list|()
argument_list|)
expr_stmt|;
block|}
specifier|private
name|void
name|runInternal
parameter_list|(
name|String
name|command
parameter_list|,
name|boolean
name|alreadyCompiled
parameter_list|)
throws|throws
name|CommandProcessorResponse
block|{
name|errorMessage
operator|=
literal|null
expr_stmt|;
name|SQLState
operator|=
literal|null
expr_stmt|;
name|downstreamError
operator|=
literal|null
expr_stmt|;
name|LockedDriverState
operator|.
name|setLockedDriverState
argument_list|(
name|lDrvState
argument_list|)
expr_stmt|;
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
if|if
condition|(
name|alreadyCompiled
condition|)
block|{
if|if
condition|(
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|COMPILED
condition|)
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|DriverState
operator|.
name|EXECUTING
expr_stmt|;
block|}
else|else
block|{
name|errorMessage
operator|=
literal|"FAILED: Precompiled query has been cancelled or closed."
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|12
argument_list|)
throw|;
block|}
block|}
else|else
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|DriverState
operator|.
name|COMPILING
expr_stmt|;
block|}
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
comment|// a flag that helps to set the correct driver state in finally block by tracking if
comment|// the method has been returned by an error or not.
name|boolean
name|isFinishedWithError
init|=
literal|true
decl_stmt|;
try|try
block|{
name|HiveDriverRunHookContext
name|hookContext
init|=
operator|new
name|HiveDriverRunHookContextImpl
argument_list|(
name|conf
argument_list|,
name|alreadyCompiled
condition|?
name|ctx
operator|.
name|getCmd
argument_list|()
else|:
name|command
argument_list|)
decl_stmt|;
comment|// Get all the driver run hooks and pre-execute them.
try|try
block|{
name|hookRunner
operator|.
name|runPreDriverHooks
argument_list|(
name|hookContext
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|errorMessage
operator|=
literal|"FAILED: Hive Internal Error: "
operator|+
name|Utilities
operator|.
name|getNameMessage
argument_list|(
name|e
argument_list|)
expr_stmt|;
name|SQLState
operator|=
name|ErrorMsg
operator|.
name|findSQLState
argument_list|(
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
name|downstreamError
operator|=
name|e
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
operator|+
literal|"\n"
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|12
argument_list|)
throw|;
block|}
if|if
condition|(
operator|!
name|alreadyCompiled
condition|)
block|{
comment|// compile internal will automatically reset the perf logger
name|compileInternal
argument_list|(
name|command
argument_list|,
literal|true
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|// Since we're reusing the compiled plan, we need to update its start time for current run
name|plan
operator|.
name|setQueryStartTime
argument_list|(
name|queryDisplay
operator|.
name|getQueryStartTime
argument_list|()
argument_list|)
expr_stmt|;
block|}
comment|//Reset the PerfLogger so that it doesn't retain any previous values.
comment|// Any value from compilation phase can be obtained through the map set in queryDisplay during compilation.
name|PerfLogger
name|perfLogger
init|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|(
literal|true
argument_list|)
decl_stmt|;
comment|// the reason that we set the txn manager for the cxt here is because each
comment|// query has its own ctx object. The txn mgr is shared across the
comment|// same instance of Driver, which can run multiple queries.
name|ctx
operator|.
name|setHiveTxnManager
argument_list|(
name|queryTxnMgr
argument_list|)
expr_stmt|;
name|checkInterrupted
argument_list|(
literal|"at acquiring the lock."
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|)
expr_stmt|;
name|lockAndRespond
argument_list|()
expr_stmt|;
try|try
block|{
if|if
condition|(
operator|!
name|isValidTxnListState
argument_list|()
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Compiling after acquiring locks"
argument_list|)
expr_stmt|;
comment|// Snapshot was outdated when locks were acquired, hence regenerate context,
comment|// txn list and retry
comment|// TODO: Lock acquisition should be moved before analyze, this is a bit hackish.
comment|// Currently, we acquire a snapshot, we compile the query wrt that snapshot,
comment|// and then, we acquire locks. If snapshot is still valid, we continue as usual.
comment|// But if snapshot is not valid, we recompile the query.
name|retrial
operator|=
literal|true
expr_stmt|;
name|backupContext
operator|.
name|addRewrittenStatementContext
argument_list|(
name|ctx
argument_list|)
expr_stmt|;
name|backupContext
operator|.
name|setHiveLocks
argument_list|(
name|ctx
operator|.
name|getHiveLocks
argument_list|()
argument_list|)
expr_stmt|;
name|ctx
operator|=
name|backupContext
expr_stmt|;
name|conf
operator|.
name|set
argument_list|(
name|ValidTxnList
operator|.
name|VALID_TXNS_KEY
argument_list|,
name|queryTxnMgr
operator|.
name|getValidTxns
argument_list|()
operator|.
name|toString
argument_list|()
argument_list|)
expr_stmt|;
if|if
condition|(
name|plan
operator|.
name|hasAcidResourcesInQuery
argument_list|()
condition|)
block|{
name|recordValidWriteIds
argument_list|(
name|queryTxnMgr
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|alreadyCompiled
condition|)
block|{
comment|// compile internal will automatically reset the perf logger
name|compileInternal
argument_list|(
name|command
argument_list|,
literal|true
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|// Since we're reusing the compiled plan, we need to update its start time for current run
name|plan
operator|.
name|setQueryStartTime
argument_list|(
name|queryDisplay
operator|.
name|getQueryStartTime
argument_list|()
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|isValidTxnListState
argument_list|()
condition|)
block|{
comment|// Throw exception
throw|throw
name|handleHiveException
argument_list|(
operator|new
name|HiveException
argument_list|(
literal|"Operation could not be executed"
argument_list|)
argument_list|,
literal|14
argument_list|)
throw|;
block|}
comment|//Reset the PerfLogger
name|perfLogger
operator|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|(
literal|true
argument_list|)
expr_stmt|;
comment|// the reason that we set the txn manager for the cxt here is because each
comment|// query has its own ctx object. The txn mgr is shared across the
comment|// same instance of Driver, which can run multiple queries.
name|ctx
operator|.
name|setHiveTxnManager
argument_list|(
name|queryTxnMgr
argument_list|)
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
throw|throw
name|handleHiveException
argument_list|(
name|e
argument_list|,
literal|13
argument_list|)
throw|;
block|}
try|try
block|{
name|execute
argument_list|()
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
block|{
name|rollback
argument_list|(
name|cpr
argument_list|)
expr_stmt|;
throw|throw
name|cpr
throw|;
block|}
comment|//if needRequireLock is false, the release here will do nothing because there is no lock
try|try
block|{
comment|//since set autocommit starts an implicit txn, close it
if|if
condition|(
name|queryTxnMgr
operator|.
name|isImplicitTransactionOpen
argument_list|()
operator|||
name|plan
operator|.
name|getOperation
argument_list|()
operator|==
name|HiveOperation
operator|.
name|COMMIT
condition|)
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|true
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|plan
operator|.
name|getOperation
argument_list|()
operator|==
name|HiveOperation
operator|.
name|ROLLBACK
condition|)
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|false
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|queryTxnMgr
operator|.
name|isTxnOpen
argument_list|()
operator|&&
name|queryState
operator|.
name|getHiveOperation
argument_list|()
operator|==
name|HiveOperation
operator|.
name|REPLLOAD
condition|)
block|{
comment|// repl load during migration, commits the explicit txn and start some internal txns. Call
comment|// releaseLocksAndCommitOrRollback to do the clean up.
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|false
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|//txn (if there is one started) is not finished
block|}
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
throw|throw
name|handleHiveException
argument_list|(
name|e
argument_list|,
literal|12
argument_list|)
throw|;
block|}
name|queryDisplay
operator|.
name|setPerfLogStarts
argument_list|(
name|QueryDisplay
operator|.
name|Phase
operator|.
name|EXECUTION
argument_list|,
name|perfLogger
operator|.
name|getStartTimes
argument_list|()
argument_list|)
expr_stmt|;
name|queryDisplay
operator|.
name|setPerfLogEnds
argument_list|(
name|QueryDisplay
operator|.
name|Phase
operator|.
name|EXECUTION
argument_list|,
name|perfLogger
operator|.
name|getEndTimes
argument_list|()
argument_list|)
expr_stmt|;
comment|// Take all the driver run hooks and post-execute them.
try|try
block|{
name|hookRunner
operator|.
name|runPostDriverHooks
argument_list|(
name|hookContext
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|errorMessage
operator|=
literal|"FAILED: Hive Internal Error: "
operator|+
name|Utilities
operator|.
name|getNameMessage
argument_list|(
name|e
argument_list|)
expr_stmt|;
name|SQLState
operator|=
name|ErrorMsg
operator|.
name|findSQLState
argument_list|(
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
name|downstreamError
operator|=
name|e
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
operator|+
literal|"\n"
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|12
argument_list|)
throw|;
block|}
name|isFinishedWithError
operator|=
literal|false
expr_stmt|;
block|}
finally|finally
block|{
if|if
condition|(
name|lDrvState
operator|.
name|isAborted
argument_list|()
condition|)
block|{
name|closeInProcess
argument_list|(
literal|true
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|// only release the related resources ctx, driverContext as normal
name|releaseResources
argument_list|()
expr_stmt|;
block|}
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|isFinishedWithError
condition|?
name|DriverState
operator|.
name|ERROR
else|:
name|DriverState
operator|.
name|EXECUTED
expr_stmt|;
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
block|}
block|}
specifier|private
name|CommandProcessorResponse
name|rollback
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
throws|throws
name|CommandProcessorResponse
block|{
comment|//console.printError(cpr.toString());
try|try
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|false
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|error
argument_list|(
literal|"rollback() FAILED: "
operator|+
name|cpr
argument_list|)
expr_stmt|;
comment|//make sure not to loose
name|handleHiveException
argument_list|(
name|e
argument_list|,
literal|12
argument_list|,
literal|"Additional info in hive.log at \"rollback() FAILED\""
argument_list|)
expr_stmt|;
block|}
return|return
name|cpr
return|;
block|}
specifier|private
name|CommandProcessorResponse
name|handleHiveException
parameter_list|(
name|HiveException
name|e
parameter_list|,
name|int
name|ret
parameter_list|)
throws|throws
name|CommandProcessorResponse
block|{
return|return
name|handleHiveException
argument_list|(
name|e
argument_list|,
name|ret
argument_list|,
literal|null
argument_list|)
return|;
block|}
specifier|private
name|CommandProcessorResponse
name|handleHiveException
parameter_list|(
name|HiveException
name|e
parameter_list|,
name|int
name|ret
parameter_list|,
name|String
name|rootMsg
parameter_list|)
throws|throws
name|CommandProcessorResponse
block|{
name|errorMessage
operator|=
literal|"FAILED: Hive Internal Error: "
operator|+
name|Utilities
operator|.
name|getNameMessage
argument_list|(
name|e
argument_list|)
expr_stmt|;
if|if
condition|(
name|rootMsg
operator|!=
literal|null
condition|)
block|{
name|errorMessage
operator|+=
literal|"\n"
operator|+
name|rootMsg
expr_stmt|;
block|}
name|SQLState
operator|=
name|e
operator|.
name|getCanonicalErrorMsg
argument_list|()
operator|!=
literal|null
condition|?
name|e
operator|.
name|getCanonicalErrorMsg
argument_list|()
operator|.
name|getSQLState
argument_list|()
else|:
name|ErrorMsg
operator|.
name|findSQLState
argument_list|(
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
name|downstreamError
operator|=
name|e
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
operator|+
literal|"\n"
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
name|ret
argument_list|)
throw|;
block|}
specifier|private
name|boolean
name|requiresLock
parameter_list|()
block|{
if|if
condition|(
operator|!
name|checkConcurrency
argument_list|()
condition|)
block|{
return|return
literal|false
return|;
block|}
comment|// Lock operations themselves don't require the lock.
if|if
condition|(
name|isExplicitLockOperation
argument_list|()
condition|)
block|{
return|return
literal|false
return|;
block|}
if|if
condition|(
operator|!
name|HiveConf
operator|.
name|getBoolVar
argument_list|(
name|conf
argument_list|,
name|ConfVars
operator|.
name|HIVE_LOCK_MAPRED_ONLY
argument_list|)
condition|)
block|{
return|return
literal|true
return|;
block|}
name|Queue
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
name|taskQueue
init|=
operator|new
name|LinkedList
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
argument_list|()
decl_stmt|;
name|taskQueue
operator|.
name|addAll
argument_list|(
name|plan
operator|.
name|getRootTasks
argument_list|()
argument_list|)
expr_stmt|;
while|while
condition|(
name|taskQueue
operator|.
name|peek
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|tsk
init|=
name|taskQueue
operator|.
name|remove
argument_list|()
decl_stmt|;
if|if
condition|(
name|tsk
operator|.
name|requireLock
argument_list|()
condition|)
block|{
return|return
literal|true
return|;
block|}
if|if
condition|(
name|tsk
operator|instanceof
name|ConditionalTask
condition|)
block|{
name|taskQueue
operator|.
name|addAll
argument_list|(
operator|(
operator|(
name|ConditionalTask
operator|)
name|tsk
operator|)
operator|.
name|getListTasks
argument_list|()
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tsk
operator|.
name|getChildTasks
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|taskQueue
operator|.
name|addAll
argument_list|(
name|tsk
operator|.
name|getChildTasks
argument_list|()
argument_list|)
expr_stmt|;
block|}
comment|// does not add back up task here, because back up task should be the same
comment|// type of the original task.
block|}
return|return
literal|false
return|;
block|}
specifier|private
name|boolean
name|isExplicitLockOperation
parameter_list|()
block|{
name|HiveOperation
name|currentOpt
init|=
name|plan
operator|.
name|getOperation
argument_list|()
decl_stmt|;
if|if
condition|(
name|currentOpt
operator|!=
literal|null
condition|)
block|{
switch|switch
condition|(
name|currentOpt
condition|)
block|{
case|case
name|LOCKDB
case|:
case|case
name|UNLOCKDB
case|:
case|case
name|LOCKTABLE
case|:
case|case
name|UNLOCKTABLE
case|:
return|return
literal|true
return|;
default|default:
return|return
literal|false
return|;
block|}
block|}
return|return
literal|false
return|;
block|}
specifier|private
name|CommandProcessorResponse
name|createProcessorResponse
parameter_list|(
name|int
name|ret
parameter_list|)
block|{
name|SessionState
operator|.
name|getPerfLogger
argument_list|()
operator|.
name|cleanupPerfLogMetrics
argument_list|()
expr_stmt|;
name|queryDisplay
operator|.
name|setErrorMessage
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
if|if
condition|(
name|downstreamError
operator|!=
literal|null
operator|&&
name|downstreamError
operator|instanceof
name|HiveException
condition|)
block|{
name|ErrorMsg
name|em
init|=
operator|(
operator|(
name|HiveException
operator|)
name|downstreamError
operator|)
operator|.
name|getCanonicalErrorMsg
argument_list|()
decl_stmt|;
if|if
condition|(
name|em
operator|!=
literal|null
condition|)
block|{
return|return
operator|new
name|CommandProcessorResponse
argument_list|(
name|ret
argument_list|,
name|errorMessage
argument_list|,
name|SQLState
argument_list|,
name|schema
argument_list|,
name|downstreamError
argument_list|,
name|em
operator|.
name|getErrorCode
argument_list|()
argument_list|,
literal|null
argument_list|)
return|;
block|}
block|}
return|return
operator|new
name|CommandProcessorResponse
argument_list|(
name|ret
argument_list|,
name|errorMessage
argument_list|,
name|SQLState
argument_list|,
name|downstreamError
argument_list|)
return|;
block|}
specifier|private
name|void
name|useFetchFromCache
parameter_list|(
name|CacheEntry
name|cacheEntry
parameter_list|)
block|{
comment|// Change query FetchTask to use new location specified in results cache.
name|FetchTask
name|fetchTaskFromCache
init|=
operator|(
name|FetchTask
operator|)
name|TaskFactory
operator|.
name|get
argument_list|(
name|cacheEntry
operator|.
name|getFetchWork
argument_list|()
argument_list|)
decl_stmt|;
name|fetchTaskFromCache
operator|.
name|initialize
argument_list|(
name|queryState
argument_list|,
name|plan
argument_list|,
literal|null
argument_list|,
name|ctx
operator|.
name|getOpContext
argument_list|()
argument_list|)
expr_stmt|;
name|plan
operator|.
name|setFetchTask
argument_list|(
name|fetchTaskFromCache
argument_list|)
expr_stmt|;
name|cacheUsage
operator|=
operator|new
name|CacheUsage
argument_list|(
name|CacheUsage
operator|.
name|CacheStatus
operator|.
name|QUERY_USING_CACHE
argument_list|,
name|cacheEntry
argument_list|)
expr_stmt|;
block|}
specifier|private
name|void
name|preExecutionCacheActions
parameter_list|()
throws|throws
name|Exception
block|{
if|if
condition|(
name|cacheUsage
operator|!=
literal|null
condition|)
block|{
if|if
condition|(
name|cacheUsage
operator|.
name|getStatus
argument_list|()
operator|==
name|CacheUsage
operator|.
name|CacheStatus
operator|.
name|CAN_CACHE_QUERY_RESULTS
operator|&&
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|ValidTxnWriteIdList
name|txnWriteIdList
init|=
literal|null
decl_stmt|;
if|if
condition|(
name|plan
operator|.
name|hasAcidResourcesInQuery
argument_list|()
condition|)
block|{
name|txnWriteIdList
operator|=
name|AcidUtils
operator|.
name|getValidTxnWriteIdList
argument_list|(
name|conf
argument_list|)
expr_stmt|;
block|}
comment|// The results of this query execution might be cacheable.
comment|// Add a placeholder entry in the cache so other queries know this result is pending.
name|CacheEntry
name|pendingCacheEntry
init|=
name|QueryResultsCache
operator|.
name|getInstance
argument_list|()
operator|.
name|addToCache
argument_list|(
name|cacheUsage
operator|.
name|getQueryInfo
argument_list|()
argument_list|,
name|txnWriteIdList
argument_list|)
decl_stmt|;
if|if
condition|(
name|pendingCacheEntry
operator|!=
literal|null
condition|)
block|{
comment|// Update cacheUsage to reference the pending entry.
name|this
operator|.
name|cacheUsage
operator|.
name|setCacheEntry
argument_list|(
name|pendingCacheEntry
argument_list|)
expr_stmt|;
block|}
block|}
block|}
block|}
specifier|private
name|void
name|postExecutionCacheActions
parameter_list|()
throws|throws
name|Exception
block|{
if|if
condition|(
name|cacheUsage
operator|!=
literal|null
condition|)
block|{
if|if
condition|(
name|cacheUsage
operator|.
name|getStatus
argument_list|()
operator|==
name|CacheUsage
operator|.
name|CacheStatus
operator|.
name|QUERY_USING_CACHE
condition|)
block|{
comment|// Using a previously cached result.
name|CacheEntry
name|cacheEntry
init|=
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
decl_stmt|;
comment|// Reader count already incremented during cache lookup.
comment|// Save to usedCacheEntry to ensure reader is released after query.
name|this
operator|.
name|usedCacheEntry
operator|=
name|cacheEntry
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|cacheUsage
operator|.
name|getStatus
argument_list|()
operator|==
name|CacheUsage
operator|.
name|CacheStatus
operator|.
name|CAN_CACHE_QUERY_RESULTS
operator|&&
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
operator|!=
literal|null
operator|&&
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|!=
literal|null
condition|)
block|{
comment|// Save results to the cache for future queries to use.
name|PerfLogger
name|perfLogger
init|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|()
decl_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|SAVE_TO_RESULTS_CACHE
argument_list|)
expr_stmt|;
name|ValidTxnWriteIdList
name|txnWriteIdList
init|=
literal|null
decl_stmt|;
if|if
condition|(
name|plan
operator|.
name|hasAcidResourcesInQuery
argument_list|()
condition|)
block|{
name|txnWriteIdList
operator|=
name|AcidUtils
operator|.
name|getValidTxnWriteIdList
argument_list|(
name|conf
argument_list|)
expr_stmt|;
block|}
name|CacheEntry
name|cacheEntry
init|=
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
decl_stmt|;
name|boolean
name|savedToCache
init|=
name|QueryResultsCache
operator|.
name|getInstance
argument_list|()
operator|.
name|setEntryValid
argument_list|(
name|cacheEntry
argument_list|,
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|.
name|getWork
argument_list|()
argument_list|)
decl_stmt|;
name|LOG
operator|.
name|info
argument_list|(
literal|"savedToCache: {} ({})"
argument_list|,
name|savedToCache
argument_list|,
name|cacheEntry
argument_list|)
expr_stmt|;
if|if
condition|(
name|savedToCache
condition|)
block|{
name|useFetchFromCache
argument_list|(
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
argument_list|)
expr_stmt|;
comment|// setEntryValid() already increments the reader count. Set usedCacheEntry so it gets released.
name|this
operator|.
name|usedCacheEntry
operator|=
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
expr_stmt|;
block|}
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|SAVE_TO_RESULTS_CACHE
argument_list|)
expr_stmt|;
block|}
block|}
block|}
specifier|private
name|void
name|execute
parameter_list|()
throws|throws
name|CommandProcessorResponse
block|{
name|PerfLogger
name|perfLogger
init|=
name|SessionState
operator|.
name|getPerfLogger
argument_list|()
decl_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|DRIVER_EXECUTE
argument_list|)
expr_stmt|;
name|boolean
name|noName
init|=
name|StringUtils
operator|.
name|isEmpty
argument_list|(
name|conf
operator|.
name|get
argument_list|(
name|MRJobConfig
operator|.
name|JOB_NAME
argument_list|)
argument_list|)
decl_stmt|;
name|int
name|maxlen
decl_stmt|;
if|if
condition|(
literal|"spark"
operator|.
name|equals
argument_list|(
name|conf
operator|.
name|getVar
argument_list|(
name|ConfVars
operator|.
name|HIVE_EXECUTION_ENGINE
argument_list|)
argument_list|)
condition|)
block|{
name|maxlen
operator|=
name|conf
operator|.
name|getIntVar
argument_list|(
name|HiveConf
operator|.
name|ConfVars
operator|.
name|HIVESPARKJOBNAMELENGTH
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|maxlen
operator|=
name|conf
operator|.
name|getIntVar
argument_list|(
name|HiveConf
operator|.
name|ConfVars
operator|.
name|HIVEJOBNAMELENGTH
argument_list|)
expr_stmt|;
block|}
name|Metrics
name|metrics
init|=
name|MetricsFactory
operator|.
name|getInstance
argument_list|()
decl_stmt|;
name|String
name|queryId
init|=
name|plan
operator|.
name|getQueryId
argument_list|()
decl_stmt|;
comment|// Get the query string from the conf file as the compileInternal() method might
comment|// hide sensitive information during query redaction.
name|String
name|queryStr
init|=
name|conf
operator|.
name|getQueryString
argument_list|()
decl_stmt|;
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
comment|// if query is not in compiled state, or executing state which is carried over from
comment|// a combined compile/execute in runInternal, throws the error
if|if
condition|(
name|lDrvState
operator|.
name|driverState
operator|!=
name|DriverState
operator|.
name|COMPILED
operator|&&
name|lDrvState
operator|.
name|driverState
operator|!=
name|DriverState
operator|.
name|EXECUTING
condition|)
block|{
name|SQLState
operator|=
literal|"HY008"
expr_stmt|;
name|errorMessage
operator|=
literal|"FAILED: unexpected driverstate: "
operator|+
name|lDrvState
operator|+
literal|", for query "
operator|+
name|queryStr
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|1000
argument_list|)
throw|;
block|}
else|else
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|DriverState
operator|.
name|EXECUTING
expr_stmt|;
block|}
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
name|maxthreads
operator|=
name|HiveConf
operator|.
name|getIntVar
argument_list|(
name|conf
argument_list|,
name|HiveConf
operator|.
name|ConfVars
operator|.
name|EXECPARALLETHREADNUMBER
argument_list|)
expr_stmt|;
name|HookContext
name|hookContext
init|=
literal|null
decl_stmt|;
comment|// Whether there's any error occurred during query execution. Used for query lifetime hook.
name|boolean
name|executionError
init|=
literal|false
decl_stmt|;
try|try
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Executing command(queryId="
operator|+
name|queryId
operator|+
literal|"): "
operator|+
name|queryStr
argument_list|)
expr_stmt|;
comment|// compile and execute can get called from different threads in case of HS2
comment|// so clear timing in this thread's Hive object before proceeding.
name|Hive
operator|.
name|get
argument_list|()
operator|.
name|clearMetaCallTiming
argument_list|()
expr_stmt|;
name|plan
operator|.
name|setStarted
argument_list|()
expr_stmt|;
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|startQuery
argument_list|(
name|queryStr
argument_list|,
name|queryId
argument_list|)
expr_stmt|;
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|logPlanProgress
argument_list|(
name|plan
argument_list|)
expr_stmt|;
block|}
name|resStream
operator|=
literal|null
expr_stmt|;
name|SessionState
name|ss
init|=
name|SessionState
operator|.
name|get
argument_list|()
decl_stmt|;
comment|// TODO: should this use getUserFromAuthenticator?
name|hookContext
operator|=
operator|new
name|PrivateHookContext
argument_list|(
name|plan
argument_list|,
name|queryState
argument_list|,
name|ctx
operator|.
name|getPathToCS
argument_list|()
argument_list|,
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getUserName
argument_list|()
argument_list|,
name|ss
operator|.
name|getUserIpAddress
argument_list|()
argument_list|,
name|InetAddress
operator|.
name|getLocalHost
argument_list|()
operator|.
name|getHostAddress
argument_list|()
argument_list|,
name|operationId
argument_list|,
name|ss
operator|.
name|getSessionId
argument_list|()
argument_list|,
name|Thread
operator|.
name|currentThread
argument_list|()
operator|.
name|getName
argument_list|()
argument_list|,
name|ss
operator|.
name|isHiveServerQuery
argument_list|()
argument_list|,
name|perfLogger
argument_list|,
name|queryInfo
argument_list|,
name|ctx
argument_list|)
expr_stmt|;
name|hookContext
operator|.
name|setHookType
argument_list|(
name|HookContext
operator|.
name|HookType
operator|.
name|PRE_EXEC_HOOK
argument_list|)
expr_stmt|;
name|hookRunner
operator|.
name|runPreHooks
argument_list|(
name|hookContext
argument_list|)
expr_stmt|;
comment|// Trigger query hooks before query execution.
name|hookRunner
operator|.
name|runBeforeExecutionHook
argument_list|(
name|queryStr
argument_list|,
name|hookContext
argument_list|)
expr_stmt|;
name|setQueryDisplays
argument_list|(
name|plan
operator|.
name|getRootTasks
argument_list|()
argument_list|)
expr_stmt|;
name|int
name|mrJobs
init|=
name|Utilities
operator|.
name|getMRTasks
argument_list|(
name|plan
operator|.
name|getRootTasks
argument_list|()
argument_list|)
operator|.
name|size
argument_list|()
decl_stmt|;
name|int
name|jobs
init|=
name|mrJobs
operator|+
name|Utilities
operator|.
name|getTezTasks
argument_list|(
name|plan
operator|.
name|getRootTasks
argument_list|()
argument_list|)
operator|.
name|size
argument_list|()
operator|+
name|Utilities
operator|.
name|getSparkTasks
argument_list|(
name|plan
operator|.
name|getRootTasks
argument_list|()
argument_list|)
operator|.
name|size
argument_list|()
decl_stmt|;
if|if
condition|(
name|jobs
operator|>
literal|0
condition|)
block|{
name|logMrWarning
argument_list|(
name|mrJobs
argument_list|)
expr_stmt|;
name|console
operator|.
name|printInfo
argument_list|(
literal|"Query ID = "
operator|+
name|queryId
argument_list|)
expr_stmt|;
name|console
operator|.
name|printInfo
argument_list|(
literal|"Total jobs = "
operator|+
name|jobs
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|setQueryProperty
argument_list|(
name|queryId
argument_list|,
name|Keys
operator|.
name|QUERY_NUM_TASKS
argument_list|,
name|String
operator|.
name|valueOf
argument_list|(
name|jobs
argument_list|)
argument_list|)
expr_stmt|;
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|setIdToTableMap
argument_list|(
name|plan
operator|.
name|getIdToTableNameMap
argument_list|()
argument_list|)
expr_stmt|;
block|}
name|String
name|jobname
init|=
name|Utilities
operator|.
name|abbreviate
argument_list|(
name|queryStr
argument_list|,
name|maxlen
operator|-
literal|6
argument_list|)
decl_stmt|;
comment|// A runtime that launches runnable tasks as separate Threads through
comment|// TaskRunners
comment|// As soon as a task isRunnable, it is put in a queue
comment|// At any time, at most maxthreads tasks can be running
comment|// The main thread polls the TaskRunners to check if they have finished.
name|checkInterrupted
argument_list|(
literal|"before running tasks."
argument_list|,
name|hookContext
argument_list|,
name|perfLogger
argument_list|)
expr_stmt|;
name|DriverContext
name|driverCxt
init|=
operator|new
name|DriverContext
argument_list|(
name|ctx
argument_list|)
decl_stmt|;
name|driverCxt
operator|.
name|prepare
argument_list|(
name|plan
argument_list|)
expr_stmt|;
name|ctx
operator|.
name|setHDFSCleanup
argument_list|(
literal|true
argument_list|)
expr_stmt|;
name|this
operator|.
name|driverCxt
operator|=
name|driverCxt
expr_stmt|;
comment|// for canceling the query (should be bound to session?)
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|setMapRedStats
argument_list|(
operator|new
name|LinkedHashMap
argument_list|<>
argument_list|()
argument_list|)
expr_stmt|;
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|setStackTraces
argument_list|(
operator|new
name|HashMap
argument_list|<>
argument_list|()
argument_list|)
expr_stmt|;
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|setLocalMapRedErrors
argument_list|(
operator|new
name|HashMap
argument_list|<>
argument_list|()
argument_list|)
expr_stmt|;
comment|// Add root Tasks to runnable
for|for
control|(
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|tsk
range|:
name|plan
operator|.
name|getRootTasks
argument_list|()
control|)
block|{
comment|// This should never happen, if it does, it's a bug with the potential to produce
comment|// incorrect results.
assert|assert
name|tsk
operator|.
name|getParentTasks
argument_list|()
operator|==
literal|null
operator|||
name|tsk
operator|.
name|getParentTasks
argument_list|()
operator|.
name|isEmpty
argument_list|()
assert|;
name|driverCxt
operator|.
name|addToRunnable
argument_list|(
name|tsk
argument_list|)
expr_stmt|;
if|if
condition|(
name|metrics
operator|!=
literal|null
condition|)
block|{
name|tsk
operator|.
name|updateTaskMetrics
argument_list|(
name|metrics
argument_list|)
expr_stmt|;
block|}
block|}
name|preExecutionCacheActions
argument_list|()
expr_stmt|;
name|perfLogger
operator|.
name|PerfLogBegin
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|RUN_TASKS
argument_list|)
expr_stmt|;
comment|// Loop while you either have tasks running, or tasks queued up
while|while
condition|(
name|driverCxt
operator|.
name|isRunning
argument_list|()
condition|)
block|{
comment|// Launch upto maxthreads tasks
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|task
decl_stmt|;
while|while
condition|(
operator|(
name|task
operator|=
name|driverCxt
operator|.
name|getRunnable
argument_list|(
name|maxthreads
argument_list|)
operator|)
operator|!=
literal|null
condition|)
block|{
name|TaskRunner
name|runner
init|=
name|launchTask
argument_list|(
name|task
argument_list|,
name|queryId
argument_list|,
name|noName
argument_list|,
name|jobname
argument_list|,
name|jobs
argument_list|,
name|driverCxt
argument_list|)
decl_stmt|;
if|if
condition|(
operator|!
name|runner
operator|.
name|isRunning
argument_list|()
condition|)
block|{
break|break;
block|}
block|}
comment|// poll the Tasks to see which one completed
name|TaskRunner
name|tskRun
init|=
name|driverCxt
operator|.
name|pollFinished
argument_list|()
decl_stmt|;
if|if
condition|(
name|tskRun
operator|==
literal|null
condition|)
block|{
continue|continue;
block|}
comment|/*           This should be removed eventually. HIVE-17814 gives more detail           explanation of whats happening and HIVE-17815 as to why this is done.           Briefly for replication the graph is huge and so memory pressure is going to be huge if           we keep a lot of references around.         */
name|String
name|opName
init|=
name|plan
operator|.
name|getOperationName
argument_list|()
decl_stmt|;
name|boolean
name|isReplicationOperation
init|=
name|opName
operator|.
name|equals
argument_list|(
name|HiveOperation
operator|.
name|REPLDUMP
operator|.
name|getOperationName
argument_list|()
argument_list|)
operator|||
name|opName
operator|.
name|equals
argument_list|(
name|HiveOperation
operator|.
name|REPLLOAD
operator|.
name|getOperationName
argument_list|()
argument_list|)
decl_stmt|;
if|if
condition|(
operator|!
name|isReplicationOperation
condition|)
block|{
name|hookContext
operator|.
name|addCompleteTask
argument_list|(
name|tskRun
argument_list|)
expr_stmt|;
block|}
name|queryDisplay
operator|.
name|setTaskResult
argument_list|(
name|tskRun
operator|.
name|getTask
argument_list|()
operator|.
name|getId
argument_list|()
argument_list|,
name|tskRun
operator|.
name|getTaskResult
argument_list|()
argument_list|)
expr_stmt|;
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|tsk
init|=
name|tskRun
operator|.
name|getTask
argument_list|()
decl_stmt|;
name|TaskResult
name|result
init|=
name|tskRun
operator|.
name|getTaskResult
argument_list|()
decl_stmt|;
name|int
name|exitVal
init|=
name|result
operator|.
name|getExitVal
argument_list|()
decl_stmt|;
name|checkInterrupted
argument_list|(
literal|"when checking the execution result."
argument_list|,
name|hookContext
argument_list|,
name|perfLogger
argument_list|)
expr_stmt|;
if|if
condition|(
name|exitVal
operator|!=
literal|0
condition|)
block|{
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|backupTask
init|=
name|tsk
operator|.
name|getAndInitBackupTask
argument_list|()
decl_stmt|;
if|if
condition|(
name|backupTask
operator|!=
literal|null
condition|)
block|{
name|setErrorMsgAndDetail
argument_list|(
name|exitVal
argument_list|,
name|result
operator|.
name|getTaskError
argument_list|()
argument_list|,
name|tsk
argument_list|)
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
name|errorMessage
operator|=
literal|"ATTEMPT: Execute BackupTask: "
operator|+
name|backupTask
operator|.
name|getClass
argument_list|()
operator|.
name|getName
argument_list|()
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
comment|// add backup task to runnable
if|if
condition|(
name|DriverContext
operator|.
name|isLaunchable
argument_list|(
name|backupTask
argument_list|)
condition|)
block|{
name|driverCxt
operator|.
name|addToRunnable
argument_list|(
name|backupTask
argument_list|)
expr_stmt|;
block|}
continue|continue;
block|}
else|else
block|{
name|setErrorMsgAndDetail
argument_list|(
name|exitVal
argument_list|,
name|result
operator|.
name|getTaskError
argument_list|()
argument_list|,
name|tsk
argument_list|)
expr_stmt|;
if|if
condition|(
name|driverCxt
operator|.
name|isShutdown
argument_list|()
condition|)
block|{
name|errorMessage
operator|=
literal|"FAILED: Operation cancelled. "
operator|+
name|errorMessage
expr_stmt|;
block|}
name|invokeFailureHooks
argument_list|(
name|perfLogger
argument_list|,
name|hookContext
argument_list|,
name|errorMessage
operator|+
name|Strings
operator|.
name|nullToEmpty
argument_list|(
name|tsk
operator|.
name|getDiagnosticsMessage
argument_list|()
argument_list|)
argument_list|,
name|result
operator|.
name|getTaskError
argument_list|()
argument_list|)
expr_stmt|;
name|SQLState
operator|=
literal|"08S01"
expr_stmt|;
comment|// 08S01 (Communication error) is the default sql state.  Override the sqlstate
comment|// based on the ErrorMsg set in HiveException.
if|if
condition|(
name|result
operator|.
name|getTaskError
argument_list|()
operator|instanceof
name|HiveException
condition|)
block|{
name|ErrorMsg
name|errorMsg
init|=
operator|(
operator|(
name|HiveException
operator|)
name|result
operator|.
name|getTaskError
argument_list|()
operator|)
operator|.
name|getCanonicalErrorMsg
argument_list|()
decl_stmt|;
if|if
condition|(
name|errorMsg
operator|!=
name|ErrorMsg
operator|.
name|GENERIC_ERROR
condition|)
block|{
name|SQLState
operator|=
name|errorMsg
operator|.
name|getSQLState
argument_list|()
expr_stmt|;
block|}
block|}
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
name|driverCxt
operator|.
name|shutdown
argument_list|()
expr_stmt|;
comment|// in case we decided to run everything in local mode, restore the
comment|// the jobtracker setting to its initial value
name|ctx
operator|.
name|restoreOriginalTracker
argument_list|()
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
name|exitVal
argument_list|)
throw|;
block|}
block|}
name|driverCxt
operator|.
name|finished
argument_list|(
name|tskRun
argument_list|)
expr_stmt|;
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|setTaskProperty
argument_list|(
name|queryId
argument_list|,
name|tsk
operator|.
name|getId
argument_list|()
argument_list|,
name|Keys
operator|.
name|TASK_RET_CODE
argument_list|,
name|String
operator|.
name|valueOf
argument_list|(
name|exitVal
argument_list|)
argument_list|)
expr_stmt|;
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|endTask
argument_list|(
name|queryId
argument_list|,
name|tsk
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tsk
operator|.
name|getChildTasks
argument_list|()
operator|!=
literal|null
condition|)
block|{
for|for
control|(
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|child
range|:
name|tsk
operator|.
name|getChildTasks
argument_list|()
control|)
block|{
if|if
condition|(
name|DriverContext
operator|.
name|isLaunchable
argument_list|(
name|child
argument_list|)
condition|)
block|{
name|driverCxt
operator|.
name|addToRunnable
argument_list|(
name|child
argument_list|)
expr_stmt|;
block|}
block|}
block|}
block|}
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|RUN_TASKS
argument_list|)
expr_stmt|;
name|postExecutionCacheActions
argument_list|()
expr_stmt|;
comment|// in case we decided to run everything in local mode, restore the
comment|// the jobtracker setting to its initial value
name|ctx
operator|.
name|restoreOriginalTracker
argument_list|()
expr_stmt|;
if|if
condition|(
name|driverCxt
operator|.
name|isShutdown
argument_list|()
condition|)
block|{
name|SQLState
operator|=
literal|"HY008"
expr_stmt|;
name|errorMessage
operator|=
literal|"FAILED: Operation cancelled"
expr_stmt|;
name|invokeFailureHooks
argument_list|(
name|perfLogger
argument_list|,
name|hookContext
argument_list|,
name|errorMessage
argument_list|,
literal|null
argument_list|)
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|1000
argument_list|)
throw|;
block|}
comment|// remove incomplete outputs.
comment|// Some incomplete outputs may be added at the beginning, for eg: for dynamic partitions.
comment|// remove them
name|HashSet
argument_list|<
name|WriteEntity
argument_list|>
name|remOutputs
init|=
operator|new
name|LinkedHashSet
argument_list|<
name|WriteEntity
argument_list|>
argument_list|()
decl_stmt|;
for|for
control|(
name|WriteEntity
name|output
range|:
name|plan
operator|.
name|getOutputs
argument_list|()
control|)
block|{
if|if
condition|(
operator|!
name|output
operator|.
name|isComplete
argument_list|()
condition|)
block|{
name|remOutputs
operator|.
name|add
argument_list|(
name|output
argument_list|)
expr_stmt|;
block|}
block|}
for|for
control|(
name|WriteEntity
name|output
range|:
name|remOutputs
control|)
block|{
name|plan
operator|.
name|getOutputs
argument_list|()
operator|.
name|remove
argument_list|(
name|output
argument_list|)
expr_stmt|;
block|}
name|hookContext
operator|.
name|setHookType
argument_list|(
name|HookContext
operator|.
name|HookType
operator|.
name|POST_EXEC_HOOK
argument_list|)
expr_stmt|;
name|hookRunner
operator|.
name|runPostExecHooks
argument_list|(
name|hookContext
argument_list|)
expr_stmt|;
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|setQueryProperty
argument_list|(
name|queryId
argument_list|,
name|Keys
operator|.
name|QUERY_RET_CODE
argument_list|,
name|String
operator|.
name|valueOf
argument_list|(
literal|0
argument_list|)
argument_list|)
expr_stmt|;
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|printRowCount
argument_list|(
name|queryId
argument_list|)
expr_stmt|;
block|}
name|releasePlan
argument_list|(
name|plan
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|CommandProcessorResponse
name|cpr
parameter_list|)
block|{
name|executionError
operator|=
literal|true
expr_stmt|;
throw|throw
name|cpr
throw|;
block|}
catch|catch
parameter_list|(
name|Throwable
name|e
parameter_list|)
block|{
name|executionError
operator|=
literal|true
expr_stmt|;
name|checkInterrupted
argument_list|(
literal|"during query execution: \n"
operator|+
name|e
operator|.
name|getMessage
argument_list|()
argument_list|,
name|hookContext
argument_list|,
name|perfLogger
argument_list|)
expr_stmt|;
name|ctx
operator|.
name|restoreOriginalTracker
argument_list|()
expr_stmt|;
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|setQueryProperty
argument_list|(
name|queryId
argument_list|,
name|Keys
operator|.
name|QUERY_RET_CODE
argument_list|,
name|String
operator|.
name|valueOf
argument_list|(
literal|12
argument_list|)
argument_list|)
expr_stmt|;
block|}
comment|// TODO: do better with handling types of Exception here
name|errorMessage
operator|=
literal|"FAILED: Hive Internal Error: "
operator|+
name|Utilities
operator|.
name|getNameMessage
argument_list|(
name|e
argument_list|)
expr_stmt|;
if|if
condition|(
name|hookContext
operator|!=
literal|null
condition|)
block|{
try|try
block|{
name|invokeFailureHooks
argument_list|(
name|perfLogger
argument_list|,
name|hookContext
argument_list|,
name|errorMessage
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|t
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Failed to invoke failure hook"
argument_list|,
name|t
argument_list|)
expr_stmt|;
block|}
block|}
name|SQLState
operator|=
literal|"08S01"
expr_stmt|;
name|downstreamError
operator|=
name|e
expr_stmt|;
name|console
operator|.
name|printError
argument_list|(
name|errorMessage
operator|+
literal|"\n"
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|e
argument_list|)
argument_list|)
expr_stmt|;
throw|throw
name|createProcessorResponse
argument_list|(
literal|12
argument_list|)
throw|;
block|}
finally|finally
block|{
comment|// Trigger query hooks after query completes its execution.
try|try
block|{
name|hookRunner
operator|.
name|runAfterExecutionHook
argument_list|(
name|queryStr
argument_list|,
name|hookContext
argument_list|,
name|executionError
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Failed when invoking query after execution hook"
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|endQuery
argument_list|(
name|queryId
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|noName
condition|)
block|{
name|conf
operator|.
name|set
argument_list|(
name|MRJobConfig
operator|.
name|JOB_NAME
argument_list|,
literal|""
argument_list|)
expr_stmt|;
block|}
name|double
name|duration
init|=
name|perfLogger
operator|.
name|PerfLogEnd
argument_list|(
name|CLASS_NAME
argument_list|,
name|PerfLogger
operator|.
name|DRIVER_EXECUTE
argument_list|)
operator|/
literal|1000.00
decl_stmt|;
name|ImmutableMap
argument_list|<
name|String
argument_list|,
name|Long
argument_list|>
name|executionHMSTimings
init|=
name|dumpMetaCallTimingWithoutEx
argument_list|(
literal|"execution"
argument_list|)
decl_stmt|;
name|queryDisplay
operator|.
name|setHmsTimings
argument_list|(
name|QueryDisplay
operator|.
name|Phase
operator|.
name|EXECUTION
argument_list|,
name|executionHMSTimings
argument_list|)
expr_stmt|;
name|Map
argument_list|<
name|String
argument_list|,
name|MapRedStats
argument_list|>
name|stats
init|=
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getMapRedStats
argument_list|()
decl_stmt|;
if|if
condition|(
name|stats
operator|!=
literal|null
operator|&&
operator|!
name|stats
operator|.
name|isEmpty
argument_list|()
condition|)
block|{
name|long
name|totalCpu
init|=
literal|0
decl_stmt|;
name|long
name|numModifiedRows
init|=
literal|0
decl_stmt|;
name|console
operator|.
name|printInfo
argument_list|(
literal|"MapReduce Jobs Launched: "
argument_list|)
expr_stmt|;
for|for
control|(
name|Map
operator|.
name|Entry
argument_list|<
name|String
argument_list|,
name|MapRedStats
argument_list|>
name|entry
range|:
name|stats
operator|.
name|entrySet
argument_list|()
control|)
block|{
name|console
operator|.
name|printInfo
argument_list|(
literal|"Stage-"
operator|+
name|entry
operator|.
name|getKey
argument_list|()
operator|+
literal|": "
operator|+
name|entry
operator|.
name|getValue
argument_list|()
argument_list|)
expr_stmt|;
name|totalCpu
operator|+=
name|entry
operator|.
name|getValue
argument_list|()
operator|.
name|getCpuMSec
argument_list|()
expr_stmt|;
if|if
condition|(
name|numModifiedRows
operator|>
operator|-
literal|1
condition|)
block|{
comment|//if overflow, then numModifiedRows is set as -1. Else update numModifiedRows with the sum.
name|numModifiedRows
operator|=
name|addWithOverflowCheck
argument_list|(
name|numModifiedRows
argument_list|,
name|entry
operator|.
name|getValue
argument_list|()
operator|.
name|getNumModifiedRows
argument_list|()
argument_list|)
expr_stmt|;
block|}
block|}
name|queryState
operator|.
name|setNumModifiedRows
argument_list|(
name|numModifiedRows
argument_list|)
expr_stmt|;
name|console
operator|.
name|printInfo
argument_list|(
literal|"Total MapReduce CPU Time Spent: "
operator|+
name|Utilities
operator|.
name|formatMsecToStr
argument_list|(
name|totalCpu
argument_list|)
argument_list|)
expr_stmt|;
block|}
name|SparkSession
name|ss
init|=
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getSparkSession
argument_list|()
decl_stmt|;
if|if
condition|(
name|ss
operator|!=
literal|null
condition|)
block|{
name|ss
operator|.
name|onQueryCompletion
argument_list|(
name|queryId
argument_list|)
expr_stmt|;
block|}
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|executionError
condition|?
name|DriverState
operator|.
name|ERROR
else|:
name|DriverState
operator|.
name|EXECUTED
expr_stmt|;
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|lDrvState
operator|.
name|isAborted
argument_list|()
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Executing command(queryId="
operator|+
name|queryId
operator|+
literal|") has been interrupted after "
operator|+
name|duration
operator|+
literal|" seconds"
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Completed executing command(queryId="
operator|+
name|queryId
operator|+
literal|"); Time taken: "
operator|+
name|duration
operator|+
literal|" seconds"
argument_list|)
expr_stmt|;
block|}
block|}
block|}
specifier|private
name|long
name|addWithOverflowCheck
parameter_list|(
name|long
name|val1
parameter_list|,
name|long
name|val2
parameter_list|)
block|{
try|try
block|{
return|return
name|Math
operator|.
name|addExact
argument_list|(
name|val1
argument_list|,
name|val2
argument_list|)
return|;
block|}
catch|catch
parameter_list|(
name|ArithmeticException
name|e
parameter_list|)
block|{
return|return
operator|-
literal|1
return|;
block|}
block|}
specifier|private
name|void
name|releasePlan
parameter_list|(
name|QueryPlan
name|plan
parameter_list|)
block|{
comment|// Plan maybe null if Driver.close is called in another thread for the same Driver object
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
if|if
condition|(
name|plan
operator|!=
literal|null
condition|)
block|{
name|plan
operator|.
name|setDone
argument_list|()
expr_stmt|;
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
try|try
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|logPlanProgress
argument_list|(
name|plan
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
comment|// Log and ignore
name|LOG
operator|.
name|warn
argument_list|(
literal|"Could not log query plan progress"
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
block|}
block|}
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
block|}
specifier|private
name|void
name|setQueryDisplays
parameter_list|(
name|List
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
name|tasks
parameter_list|)
block|{
if|if
condition|(
name|tasks
operator|!=
literal|null
condition|)
block|{
name|Set
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
name|visited
init|=
operator|new
name|HashSet
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
argument_list|()
decl_stmt|;
while|while
condition|(
operator|!
name|tasks
operator|.
name|isEmpty
argument_list|()
condition|)
block|{
name|tasks
operator|=
name|setQueryDisplays
argument_list|(
name|tasks
argument_list|,
name|visited
argument_list|)
expr_stmt|;
block|}
block|}
block|}
specifier|private
name|List
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
name|setQueryDisplays
parameter_list|(
name|List
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
name|tasks
parameter_list|,
name|Set
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
name|visited
parameter_list|)
block|{
name|List
argument_list|<
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
argument_list|>
name|childTasks
init|=
operator|new
name|ArrayList
argument_list|<>
argument_list|()
decl_stmt|;
for|for
control|(
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|task
range|:
name|tasks
control|)
block|{
if|if
condition|(
name|visited
operator|.
name|contains
argument_list|(
name|task
argument_list|)
condition|)
block|{
continue|continue;
block|}
name|task
operator|.
name|setQueryDisplay
argument_list|(
name|queryDisplay
argument_list|)
expr_stmt|;
if|if
condition|(
name|task
operator|.
name|getDependentTasks
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|childTasks
operator|.
name|addAll
argument_list|(
name|task
operator|.
name|getDependentTasks
argument_list|()
argument_list|)
expr_stmt|;
block|}
name|visited
operator|.
name|add
argument_list|(
name|task
argument_list|)
expr_stmt|;
block|}
return|return
name|childTasks
return|;
block|}
specifier|private
name|void
name|logMrWarning
parameter_list|(
name|int
name|mrJobs
parameter_list|)
block|{
if|if
condition|(
name|mrJobs
operator|<=
literal|0
operator|||
operator|!
operator|(
literal|"mr"
operator|.
name|equals
argument_list|(
name|HiveConf
operator|.
name|getVar
argument_list|(
name|conf
argument_list|,
name|ConfVars
operator|.
name|HIVE_EXECUTION_ENGINE
argument_list|)
argument_list|)
operator|)
condition|)
block|{
return|return;
block|}
name|String
name|warning
init|=
name|HiveConf
operator|.
name|generateMrDeprecationWarning
argument_list|()
decl_stmt|;
name|LOG
operator|.
name|warn
argument_list|(
name|warning
argument_list|)
expr_stmt|;
block|}
specifier|private
name|void
name|setErrorMsgAndDetail
parameter_list|(
name|int
name|exitVal
parameter_list|,
name|Throwable
name|downstreamError
parameter_list|,
name|Task
name|tsk
parameter_list|)
block|{
name|this
operator|.
name|downstreamError
operator|=
name|downstreamError
expr_stmt|;
name|errorMessage
operator|=
literal|"FAILED: Execution Error, return code "
operator|+
name|exitVal
operator|+
literal|" from "
operator|+
name|tsk
operator|.
name|getClass
argument_list|()
operator|.
name|getName
argument_list|()
expr_stmt|;
if|if
condition|(
name|downstreamError
operator|!=
literal|null
condition|)
block|{
comment|//here we assume that upstream code may have parametrized the msg from ErrorMsg
comment|//so we want to keep it
if|if
condition|(
name|downstreamError
operator|.
name|getMessage
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|errorMessage
operator|+=
literal|". "
operator|+
name|downstreamError
operator|.
name|getMessage
argument_list|()
expr_stmt|;
block|}
else|else
block|{
name|errorMessage
operator|+=
literal|". "
operator|+
name|org
operator|.
name|apache
operator|.
name|hadoop
operator|.
name|util
operator|.
name|StringUtils
operator|.
name|stringifyException
argument_list|(
name|downstreamError
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
name|ErrorMsg
name|em
init|=
name|ErrorMsg
operator|.
name|getErrorMsg
argument_list|(
name|exitVal
argument_list|)
decl_stmt|;
if|if
condition|(
name|em
operator|!=
literal|null
condition|)
block|{
name|errorMessage
operator|+=
literal|". "
operator|+
name|em
operator|.
name|getMsg
argument_list|()
expr_stmt|;
block|}
block|}
block|}
specifier|private
name|void
name|invokeFailureHooks
parameter_list|(
name|PerfLogger
name|perfLogger
parameter_list|,
name|HookContext
name|hookContext
parameter_list|,
name|String
name|errorMessage
parameter_list|,
name|Throwable
name|exception
parameter_list|)
throws|throws
name|Exception
block|{
name|hookContext
operator|.
name|setHookType
argument_list|(
name|HookContext
operator|.
name|HookType
operator|.
name|ON_FAILURE_HOOK
argument_list|)
expr_stmt|;
name|hookContext
operator|.
name|setErrorMessage
argument_list|(
name|errorMessage
argument_list|)
expr_stmt|;
name|hookContext
operator|.
name|setException
argument_list|(
name|exception
argument_list|)
expr_stmt|;
comment|// Get all the failure execution hooks and execute them.
name|hookRunner
operator|.
name|runFailureHooks
argument_list|(
name|hookContext
argument_list|)
expr_stmt|;
block|}
comment|/**    * Launches a new task    *    * @param tsk    *          task being launched    * @param queryId    *          Id of the query containing the task    * @param noName    *          whether the task has a name set    * @param jobname    *          name of the task, if it is a map-reduce job    * @param jobs    *          number of map-reduce jobs    * @param cxt    *          the driver context    */
specifier|private
name|TaskRunner
name|launchTask
parameter_list|(
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|tsk
parameter_list|,
name|String
name|queryId
parameter_list|,
name|boolean
name|noName
parameter_list|,
name|String
name|jobname
parameter_list|,
name|int
name|jobs
parameter_list|,
name|DriverContext
name|cxt
parameter_list|)
throws|throws
name|HiveException
block|{
if|if
condition|(
name|SessionState
operator|.
name|get
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|SessionState
operator|.
name|get
argument_list|()
operator|.
name|getHiveHistory
argument_list|()
operator|.
name|startTask
argument_list|(
name|queryId
argument_list|,
name|tsk
argument_list|,
name|tsk
operator|.
name|getClass
argument_list|()
operator|.
name|getName
argument_list|()
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tsk
operator|.
name|isMapRedTask
argument_list|()
operator|&&
operator|!
operator|(
name|tsk
operator|instanceof
name|ConditionalTask
operator|)
condition|)
block|{
if|if
condition|(
name|noName
condition|)
block|{
name|conf
operator|.
name|set
argument_list|(
name|MRJobConfig
operator|.
name|JOB_NAME
argument_list|,
name|jobname
operator|+
literal|" ("
operator|+
name|tsk
operator|.
name|getId
argument_list|()
operator|+
literal|")"
argument_list|)
expr_stmt|;
block|}
name|conf
operator|.
name|set
argument_list|(
name|DagUtils
operator|.
name|MAPREDUCE_WORKFLOW_NODE_NAME
argument_list|,
name|tsk
operator|.
name|getId
argument_list|()
argument_list|)
expr_stmt|;
name|Utilities
operator|.
name|setWorkflowAdjacencies
argument_list|(
name|conf
argument_list|,
name|plan
argument_list|)
expr_stmt|;
name|cxt
operator|.
name|incCurJobNo
argument_list|(
literal|1
argument_list|)
expr_stmt|;
name|console
operator|.
name|printInfo
argument_list|(
literal|"Launching Job "
operator|+
name|cxt
operator|.
name|getCurJobNo
argument_list|()
operator|+
literal|" out of "
operator|+
name|jobs
argument_list|)
expr_stmt|;
block|}
name|tsk
operator|.
name|initialize
argument_list|(
name|queryState
argument_list|,
name|plan
argument_list|,
name|cxt
argument_list|,
name|ctx
operator|.
name|getOpContext
argument_list|()
argument_list|)
expr_stmt|;
name|TaskRunner
name|tskRun
init|=
operator|new
name|TaskRunner
argument_list|(
name|tsk
argument_list|,
name|cxt
argument_list|)
decl_stmt|;
name|cxt
operator|.
name|launching
argument_list|(
name|tskRun
argument_list|)
expr_stmt|;
comment|// Launch Task
if|if
condition|(
name|HiveConf
operator|.
name|getBoolVar
argument_list|(
name|tsk
operator|.
name|getConf
argument_list|()
argument_list|,
name|HiveConf
operator|.
name|ConfVars
operator|.
name|EXECPARALLEL
argument_list|)
operator|&&
name|tsk
operator|.
name|canExecuteInParallel
argument_list|()
condition|)
block|{
comment|// Launch it in the parallel mode, as a separate thread only for MR tasks
if|if
condition|(
name|LOG
operator|.
name|isInfoEnabled
argument_list|()
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Starting task ["
operator|+
name|tsk
operator|+
literal|"] in parallel"
argument_list|)
expr_stmt|;
block|}
name|tskRun
operator|.
name|start
argument_list|()
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|LOG
operator|.
name|isInfoEnabled
argument_list|()
condition|)
block|{
name|LOG
operator|.
name|info
argument_list|(
literal|"Starting task ["
operator|+
name|tsk
operator|+
literal|"] in serial mode"
argument_list|)
expr_stmt|;
block|}
name|tskRun
operator|.
name|runSequential
argument_list|()
expr_stmt|;
block|}
return|return
name|tskRun
return|;
block|}
annotation|@
name|Override
specifier|public
name|boolean
name|isFetchingTable
parameter_list|()
block|{
return|return
name|fetchTask
operator|!=
literal|null
return|;
block|}
annotation|@
name|SuppressWarnings
argument_list|(
literal|"unchecked"
argument_list|)
annotation|@
name|Override
specifier|public
name|boolean
name|getResults
parameter_list|(
name|List
name|res
parameter_list|)
throws|throws
name|IOException
block|{
if|if
condition|(
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|DESTROYED
operator|||
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|CLOSED
condition|)
block|{
throw|throw
operator|new
name|IOException
argument_list|(
literal|"FAILED: query has been cancelled, closed, or destroyed."
argument_list|)
throw|;
block|}
if|if
condition|(
name|isFetchingTable
argument_list|()
condition|)
block|{
comment|/**        * If resultset serialization to thrift object is enabled, and if the destination table is        * indeed written using ThriftJDBCBinarySerDe, read one row from the output sequence file,        * since it is a blob of row batches.        */
if|if
condition|(
name|fetchTask
operator|.
name|getWork
argument_list|()
operator|.
name|isUsingThriftJDBCBinarySerDe
argument_list|()
condition|)
block|{
name|maxRows
operator|=
literal|1
expr_stmt|;
block|}
name|fetchTask
operator|.
name|setMaxRows
argument_list|(
name|maxRows
argument_list|)
expr_stmt|;
return|return
name|fetchTask
operator|.
name|fetch
argument_list|(
name|res
argument_list|)
return|;
block|}
if|if
condition|(
name|resStream
operator|==
literal|null
condition|)
block|{
name|resStream
operator|=
name|ctx
operator|.
name|getStream
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|resStream
operator|==
literal|null
condition|)
block|{
return|return
literal|false
return|;
block|}
name|int
name|numRows
init|=
literal|0
decl_stmt|;
name|String
name|row
init|=
literal|null
decl_stmt|;
while|while
condition|(
name|numRows
operator|<
name|maxRows
condition|)
block|{
if|if
condition|(
name|resStream
operator|==
literal|null
condition|)
block|{
if|if
condition|(
name|numRows
operator|>
literal|0
condition|)
block|{
return|return
literal|true
return|;
block|}
else|else
block|{
return|return
literal|false
return|;
block|}
block|}
name|bos
operator|.
name|reset
argument_list|()
expr_stmt|;
name|Utilities
operator|.
name|StreamStatus
name|ss
decl_stmt|;
try|try
block|{
name|ss
operator|=
name|Utilities
operator|.
name|readColumn
argument_list|(
name|resStream
argument_list|,
name|bos
argument_list|)
expr_stmt|;
if|if
condition|(
name|bos
operator|.
name|getLength
argument_list|()
operator|>
literal|0
condition|)
block|{
name|row
operator|=
operator|new
name|String
argument_list|(
name|bos
operator|.
name|getData
argument_list|()
argument_list|,
literal|0
argument_list|,
name|bos
operator|.
name|getLength
argument_list|()
argument_list|,
literal|"UTF-8"
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|ss
operator|==
name|Utilities
operator|.
name|StreamStatus
operator|.
name|TERMINATED
condition|)
block|{
name|row
operator|=
operator|new
name|String
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|row
operator|!=
literal|null
condition|)
block|{
name|numRows
operator|++
expr_stmt|;
name|res
operator|.
name|add
argument_list|(
name|row
argument_list|)
expr_stmt|;
block|}
name|row
operator|=
literal|null
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|IOException
name|e
parameter_list|)
block|{
name|console
operator|.
name|printError
argument_list|(
literal|"FAILED: Unexpected IO exception : "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
return|return
literal|false
return|;
block|}
if|if
condition|(
name|ss
operator|==
name|Utilities
operator|.
name|StreamStatus
operator|.
name|EOF
condition|)
block|{
name|resStream
operator|=
name|ctx
operator|.
name|getStream
argument_list|()
expr_stmt|;
block|}
block|}
return|return
literal|true
return|;
block|}
annotation|@
name|Override
specifier|public
name|void
name|resetFetch
parameter_list|()
throws|throws
name|IOException
block|{
if|if
condition|(
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|DESTROYED
operator|||
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|CLOSED
condition|)
block|{
throw|throw
operator|new
name|IOException
argument_list|(
literal|"FAILED: driver has been cancelled, closed or destroyed."
argument_list|)
throw|;
block|}
if|if
condition|(
name|isFetchingTable
argument_list|()
condition|)
block|{
try|try
block|{
name|fetchTask
operator|.
name|clearFetch
argument_list|()
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
throw|throw
operator|new
name|IOException
argument_list|(
literal|"Error closing the current fetch task"
argument_list|,
name|e
argument_list|)
throw|;
block|}
comment|// FetchTask should not depend on the plan.
name|fetchTask
operator|.
name|initialize
argument_list|(
name|queryState
argument_list|,
literal|null
argument_list|,
literal|null
argument_list|,
name|ctx
operator|.
name|getOpContext
argument_list|()
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|ctx
operator|.
name|resetStream
argument_list|()
expr_stmt|;
name|resStream
operator|=
literal|null
expr_stmt|;
block|}
block|}
comment|// DriverContext could be released in the query and close processes at same
comment|// time, which needs to be thread protected.
specifier|private
name|void
name|releaseDriverContext
parameter_list|()
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
if|if
condition|(
name|driverCxt
operator|!=
literal|null
condition|)
block|{
name|driverCxt
operator|.
name|shutdown
argument_list|()
expr_stmt|;
name|driverCxt
operator|=
literal|null
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|debug
argument_list|(
literal|"Exception while shutting down the task runner"
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
block|}
specifier|private
name|void
name|releasePlan
parameter_list|()
block|{
try|try
block|{
if|if
condition|(
name|plan
operator|!=
literal|null
condition|)
block|{
name|fetchTask
operator|=
name|plan
operator|.
name|getFetchTask
argument_list|()
expr_stmt|;
if|if
condition|(
name|fetchTask
operator|!=
literal|null
condition|)
block|{
name|fetchTask
operator|.
name|setDriverContext
argument_list|(
literal|null
argument_list|)
expr_stmt|;
name|fetchTask
operator|.
name|setQueryPlan
argument_list|(
literal|null
argument_list|)
expr_stmt|;
block|}
block|}
name|plan
operator|=
literal|null
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|debug
argument_list|(
literal|"Exception while clearing the Fetch task"
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
block|}
specifier|private
name|void
name|releaseContext
parameter_list|()
block|{
try|try
block|{
if|if
condition|(
name|ctx
operator|!=
literal|null
condition|)
block|{
name|boolean
name|deleteResultDir
init|=
literal|true
decl_stmt|;
comment|// don't let context delete result dirs and scratch dirs if result was cached
if|if
condition|(
name|this
operator|.
name|cacheUsage
operator|!=
literal|null
operator|&&
name|this
operator|.
name|cacheUsage
operator|.
name|getStatus
argument_list|()
operator|==
name|CacheUsage
operator|.
name|CacheStatus
operator|.
name|QUERY_USING_CACHE
condition|)
block|{
name|deleteResultDir
operator|=
literal|false
expr_stmt|;
block|}
name|ctx
operator|.
name|clear
argument_list|(
name|deleteResultDir
argument_list|)
expr_stmt|;
if|if
condition|(
name|ctx
operator|.
name|getHiveLocks
argument_list|()
operator|!=
literal|null
condition|)
block|{
name|hiveLocks
operator|.
name|addAll
argument_list|(
name|ctx
operator|.
name|getHiveLocks
argument_list|()
argument_list|)
expr_stmt|;
name|ctx
operator|.
name|setHiveLocks
argument_list|(
literal|null
argument_list|)
expr_stmt|;
block|}
name|ctx
operator|=
literal|null
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|debug
argument_list|(
literal|"Exception while clearing the context "
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
block|}
specifier|private
name|void
name|releaseResStream
parameter_list|()
block|{
try|try
block|{
if|if
condition|(
name|resStream
operator|!=
literal|null
condition|)
block|{
operator|(
operator|(
name|FSDataInputStream
operator|)
name|resStream
operator|)
operator|.
name|close
argument_list|()
expr_stmt|;
name|resStream
operator|=
literal|null
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|debug
argument_list|(
literal|" Exception while closing the resStream "
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
block|}
specifier|private
name|void
name|releaseFetchTask
parameter_list|()
block|{
try|try
block|{
if|if
condition|(
name|fetchTask
operator|!=
literal|null
condition|)
block|{
name|fetchTask
operator|.
name|clearFetch
argument_list|()
expr_stmt|;
name|fetchTask
operator|=
literal|null
expr_stmt|;
block|}
block|}
catch|catch
parameter_list|(
name|Exception
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|debug
argument_list|(
literal|" Exception while clearing the FetchTask "
argument_list|,
name|e
argument_list|)
expr_stmt|;
block|}
block|}
specifier|private
name|boolean
name|hasBadCacheAttempt
parameter_list|()
block|{
comment|// Check if the query results were cacheable, and created a pending cache entry.
comment|// If we successfully saved the results, the usage would have changed to QUERY_USING_CACHE.
return|return
operator|(
name|cacheUsage
operator|!=
literal|null
operator|&&
name|cacheUsage
operator|.
name|getStatus
argument_list|()
operator|==
name|CacheUsage
operator|.
name|CacheStatus
operator|.
name|CAN_CACHE_QUERY_RESULTS
operator|&&
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
operator|!=
literal|null
operator|)
return|;
block|}
specifier|private
name|void
name|releaseCachedResult
parameter_list|()
block|{
comment|// Assumes the reader count has been incremented automatically by the results cache by either
comment|// lookup or creating the cache entry.
if|if
condition|(
name|usedCacheEntry
operator|!=
literal|null
condition|)
block|{
name|usedCacheEntry
operator|.
name|releaseReader
argument_list|()
expr_stmt|;
name|usedCacheEntry
operator|=
literal|null
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|hasBadCacheAttempt
argument_list|()
condition|)
block|{
comment|// This query create a pending cache entry but it was never saved with real results, cleanup.
comment|// This step is required, as there may be queries waiting on this pending cache entry.
comment|// Removing/invalidating this entry will notify the waiters that this entry cannot be used.
try|try
block|{
name|QueryResultsCache
operator|.
name|getInstance
argument_list|()
operator|.
name|removeEntry
argument_list|(
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|Exception
name|err
parameter_list|)
block|{
name|LOG
operator|.
name|error
argument_list|(
literal|"Error removing failed cache entry "
operator|+
name|cacheUsage
operator|.
name|getCacheEntry
argument_list|()
argument_list|,
name|err
argument_list|)
expr_stmt|;
block|}
block|}
name|cacheUsage
operator|=
literal|null
expr_stmt|;
block|}
comment|// Close and release resources within a running query process. Since it runs under
comment|// driver state COMPILING, EXECUTING or INTERRUPT, it would not have race condition
comment|// with the releases probably running in the other closing thread.
specifier|private
name|int
name|closeInProcess
parameter_list|(
name|boolean
name|destroyed
parameter_list|)
block|{
name|releaseDriverContext
argument_list|()
expr_stmt|;
name|releasePlan
argument_list|()
expr_stmt|;
name|releaseCachedResult
argument_list|()
expr_stmt|;
name|releaseFetchTask
argument_list|()
expr_stmt|;
name|releaseResStream
argument_list|()
expr_stmt|;
name|releaseContext
argument_list|()
expr_stmt|;
if|if
condition|(
name|destroyed
condition|)
block|{
if|if
condition|(
operator|!
name|hiveLocks
operator|.
name|isEmpty
argument_list|()
condition|)
block|{
try|try
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|false
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Exception when releasing locking in destroy: "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
block|}
block|}
name|ShutdownHookManager
operator|.
name|removeShutdownHook
argument_list|(
name|shutdownRunner
argument_list|)
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
comment|// is called to stop the query if it is running, clean query results, and release resources.
annotation|@
name|Override
specifier|public
name|void
name|close
parameter_list|()
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
name|releaseDriverContext
argument_list|()
expr_stmt|;
if|if
condition|(
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|COMPILING
operator|||
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|EXECUTING
condition|)
block|{
name|lDrvState
operator|.
name|abort
argument_list|()
expr_stmt|;
block|}
name|releasePlan
argument_list|()
expr_stmt|;
name|releaseContext
argument_list|()
expr_stmt|;
name|releaseCachedResult
argument_list|()
expr_stmt|;
name|releaseFetchTask
argument_list|()
expr_stmt|;
name|releaseResStream
argument_list|()
expr_stmt|;
name|lDrvState
operator|.
name|driverState
operator|=
name|DriverState
operator|.
name|CLOSED
expr_stmt|;
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
name|LockedDriverState
operator|.
name|removeLockedDriverState
argument_list|()
expr_stmt|;
block|}
name|destroy
argument_list|()
expr_stmt|;
block|}
comment|// is usually called after close() to commit or rollback a query and end the driver life cycle.
comment|// do not understand why it is needed and wonder if it could be combined with close.
annotation|@
name|Override
specifier|public
name|void
name|destroy
parameter_list|()
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|lock
argument_list|()
expr_stmt|;
try|try
block|{
comment|// in the cancel case where the driver state is INTERRUPTED, destroy will be deferred to
comment|// the query process
if|if
condition|(
name|lDrvState
operator|.
name|driverState
operator|==
name|DriverState
operator|.
name|DESTROYED
condition|)
block|{
return|return;
block|}
else|else
block|{
name|lDrvState
operator|.
name|driverState
operator|=
name|DriverState
operator|.
name|DESTROYED
expr_stmt|;
block|}
block|}
finally|finally
block|{
name|lDrvState
operator|.
name|stateLock
operator|.
name|unlock
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|hiveLocks
operator|.
name|isEmpty
argument_list|()
condition|)
block|{
try|try
block|{
name|releaseLocksAndCommitOrRollback
argument_list|(
literal|false
argument_list|)
expr_stmt|;
block|}
catch|catch
parameter_list|(
name|LockException
name|e
parameter_list|)
block|{
name|LOG
operator|.
name|warn
argument_list|(
literal|"Exception when releasing locking in destroy: "
operator|+
name|e
operator|.
name|getMessage
argument_list|()
argument_list|)
expr_stmt|;
block|}
block|}
name|ShutdownHookManager
operator|.
name|removeShutdownHook
argument_list|(
name|shutdownRunner
argument_list|)
expr_stmt|;
block|}
specifier|public
name|String
name|getErrorMsg
parameter_list|()
block|{
return|return
name|errorMessage
return|;
block|}
annotation|@
name|Override
specifier|public
name|QueryDisplay
name|getQueryDisplay
parameter_list|()
block|{
return|return
name|queryDisplay
return|;
block|}
comment|/**    * Set the HS2 operation handle's guid string    * @param opId base64 encoded guid string    */
annotation|@
name|Override
specifier|public
name|void
name|setOperationId
parameter_list|(
name|String
name|opId
parameter_list|)
block|{
name|this
operator|.
name|operationId
operator|=
name|opId
expr_stmt|;
block|}
annotation|@
name|Override
specifier|public
name|QueryState
name|getQueryState
parameter_list|()
block|{
return|return
name|queryState
return|;
block|}
specifier|public
name|HookRunner
name|getHookRunner
parameter_list|()
block|{
return|return
name|hookRunner
return|;
block|}
specifier|public
name|void
name|setStatsSource
parameter_list|(
name|StatsSource
name|runtimeStatsSource
parameter_list|)
block|{
name|this
operator|.
name|statsSource
operator|=
name|runtimeStatsSource
expr_stmt|;
block|}
specifier|public
name|StatsSource
name|getStatsSource
parameter_list|()
block|{
return|return
name|statsSource
return|;
block|}
annotation|@
name|Override
specifier|public
name|boolean
name|hasResultSet
parameter_list|()
block|{
comment|// TODO explain should use a FetchTask for reading
for|for
control|(
name|Task
argument_list|<
name|?
extends|extends
name|Serializable
argument_list|>
name|task
range|:
name|plan
operator|.
name|getRootTasks
argument_list|()
control|)
block|{
if|if
condition|(
name|task
operator|.
name|getClass
argument_list|()
operator|==
name|ExplainTask
operator|.
name|class
condition|)
block|{
return|return
literal|true
return|;
block|}
block|}
if|if
condition|(
name|plan
operator|.
name|getFetchTask
argument_list|()
operator|!=
literal|null
operator|&&
name|schema
operator|!=
literal|null
operator|&&
name|schema
operator|.
name|isSetFieldSchemas
argument_list|()
condition|)
block|{
return|return
literal|true
return|;
block|}
else|else
block|{
return|return
literal|false
return|;
block|}
block|}
name|void
name|setCompactionWriteIds
parameter_list|(
name|ValidWriteIdList
name|val
parameter_list|,
name|long
name|compactorTxnId
parameter_list|)
block|{
name|this
operator|.
name|compactionWriteIds
operator|=
name|val
expr_stmt|;
name|this
operator|.
name|compactorTxnId
operator|=
name|compactorTxnId
expr_stmt|;
block|}
block|}
end_class

end_unit

